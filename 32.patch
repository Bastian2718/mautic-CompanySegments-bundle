From b3350648c536e54d521df7a78414f2d6b2651bb7 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Ekkehard=20G=C3=BCmbel?=
 <43146234+ekkeguembel@users.noreply.github.com>
Date: Sat, 4 Oct 2025 10:07:59 +0200
Subject: [PATCH 01/12] Update config.php

---
 Config/config.php | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/Config/config.php b/Config/config.php
index 4f8d5e0..be6ed94 100644
--- a/Config/config.php
+++ b/Config/config.php
@@ -3,7 +3,7 @@
 return [
     'name'        => 'Company Segments by Leuchtfeuer',
     'description' => 'Provide a 2nd type of Segments which can contain Companies (and allows segment filters).',
-    'version'     => '1.2.4',
+    'version'     => '1.2.5',
     'author'      => 'Leuchtfeuer Digital Marketing GmbH',
     'routes'      => [
         'main' => [

From 1947887afb21bb82b05fb0fb06ff8e107d7dd11f Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Mon, 27 Oct 2025 23:34:03 +0000
Subject: [PATCH 02/12] move to m6

---
 Command/UpdateCompanySegmentsCommand.php      |  1 +
 .../Api/CompanySegmentApiController.php       | 20 +++--
 Controller/BatchSegmentController.php         |  4 +-
 Controller/CompanySegmentController.php       |  7 +-
 Entity/CompaniesSegmentsRepository.php        | 23 ++++-
 EventListener/ReportSubscriber.php            | 42 +++++++--
 EventListener/TypeOperatorSubscriber.php      | 70 +++++++++++----
 EventListener/UpdatePluginSubscriber.php      | 10 +--
 Model/CompanySegmentActionModel.php           |  8 +-
 Model/CompanySegmentModel.php                 | 89 ++++++++++++++++---
 Segment/Query/CompanySegmentQueryBuilder.php  | 39 ++++----
 .../SegmentReferenceFilterQueryBuilder.php    | 50 ++++++++---
 Service/CompanySegmentService.php             | 40 +++++++--
 Tests/Controller/AjaxControllerTest.php       |  4 +
 .../Controller/BatchSegmentControllerTest.php |  6 ++
 .../CompanySegmentControllerTest.php          | 15 ++--
 Tests/EventListener/ReportSubscriberTest.php  | 15 +++-
 .../Api/CompanySegmentApiControllerTest.php   | 17 ++++
 .../CompanySegmentControllerTest.php          | 11 ++-
 .../CompanySegmentsPermissionsTest.php        |  8 +-
 Tests/HelperCompanySegmentTestTrait.php       | 20 +++++
 Tests/MauticMysqlTestCase.php                 |  2 +
 Tests/NewUserTrait.php                        | 10 +--
 ...SegmentReferenceFilterQueryBuilderTest.php | 22 ++++-
 Tests/Service/CompanySegmentServiceTest.php   | 48 ++++++++--
 composer.json                                 | 27 +++---
 phpstan.neon                                  |  2 +
 27 files changed, 477 insertions(+), 133 deletions(-)

diff --git a/Command/UpdateCompanySegmentsCommand.php b/Command/UpdateCompanySegmentsCommand.php
index c8f8ab5..3224eb3 100644
--- a/Command/UpdateCompanySegmentsCommand.php
+++ b/Command/UpdateCompanySegmentsCommand.php
@@ -141,6 +141,7 @@ protected function execute(InputInterface $input, OutputInterface $output): int
 
             foreach ($companySegments as $companySegment) {
                 $startTimeForSingleSegment = time();
+                assert($companySegment instanceof CompanySegment);
                 $this->rebuildSegment($companySegment, $batch, $max, $output);
                 if ($enableTimeMeasurement) {
                     $totalTime = round(microtime(true) - $startTimeForSingleSegment, 2);
diff --git a/Controller/Api/CompanySegmentApiController.php b/Controller/Api/CompanySegmentApiController.php
index 97beb4d..9bf785a 100644
--- a/Controller/Api/CompanySegmentApiController.php
+++ b/Controller/Api/CompanySegmentApiController.php
@@ -7,7 +7,6 @@
 use Doctrine\Persistence\ManagerRegistry;
 use Mautic\ApiBundle\Controller\CommonApiController;
 use Mautic\ApiBundle\Helper\EntityResultHelper;
-use Mautic\CoreBundle\Factory\MauticFactory;
 use Mautic\CoreBundle\Factory\ModelFactory;
 use Mautic\CoreBundle\Helper\AppVersion;
 use Mautic\CoreBundle\Helper\CoreParametersHelper;
@@ -39,7 +38,6 @@ public function __construct(
         ModelFactory $modelFactory,
         EventDispatcherInterface $dispatcher,
         CoreParametersHelper $coreParametersHelper,
-        MauticFactory $factory,
         private CompanySegmentModel $companySegmentModel,
     ) {
         $this->model             = $this->companySegmentModel;
@@ -58,8 +56,7 @@ public function __construct(
             $doctrine,
             $modelFactory,
             $dispatcher,
-            $coreParametersHelper,
-            $factory
+            $coreParametersHelper
         );
     }
 
@@ -114,7 +111,9 @@ public function deleteEntitiesAction(Request $request)
             return $valid;
         }
 
+        /** @var array<int, array<int|string>> $errors */
         $errors            = [];
+        /** @var array<int, CompanySegment|null> $entities */
         $entities          = $this->getBatchEntities($parameters, $errors, true);
         $ids               =  [];
         foreach ($entities as $entity) {
@@ -131,7 +130,18 @@ public function deleteEntitiesAction(Request $request)
             $result           = [];
             $result['errors'] = $errorMessage;
 
-            return $this->returnError((string) json_encode($result), Response::HTTP_PRECONDITION_FAILED);
+            try {
+                $json = json_encode($result, JSON_THROW_ON_ERROR);
+            } catch (\JsonException $e) {
+                $json = json_encode(
+                    ['errors' => $this->translator->trans('mautic.core.error.json_encode_failed')]
+                );
+                if (false === $json) {
+                    $json = '{"errors":"unknown"}';
+                }
+            }
+
+            return $this->returnError($json, Response::HTTP_PRECONDITION_FAILED);
         }
 
         $this->inBatchMode = true;
diff --git a/Controller/BatchSegmentController.php b/Controller/BatchSegmentController.php
index e74f691..1b1e385 100644
--- a/Controller/BatchSegmentController.php
+++ b/Controller/BatchSegmentController.php
@@ -6,7 +6,6 @@
 
 use Doctrine\Persistence\ManagerRegistry;
 use Mautic\CoreBundle\Controller\AbstractFormController;
-use Mautic\CoreBundle\Factory\MauticFactory;
 use Mautic\CoreBundle\Factory\ModelFactory;
 use Mautic\CoreBundle\Helper\CoreParametersHelper;
 use Mautic\CoreBundle\Helper\UserHelper;
@@ -28,7 +27,6 @@ public function __construct(
         private CompanySegmentActionModel $segmentActionModel,
         private CompanySegmentModel $segmentModel,
         ManagerRegistry $doctrine,
-        MauticFactory $factory,
         ModelFactory $modelFactory,
         UserHelper $userHelper,
         CoreParametersHelper $coreParametersHelper,
@@ -38,7 +36,7 @@ public function __construct(
         RequestStack $requestStack,
         CorePermissions $security,
     ) {
-        parent::__construct($doctrine, $factory, $modelFactory, $userHelper, $coreParametersHelper, $dispatcher, $translator, $flashBag, $requestStack, $security);
+        parent::__construct($doctrine, $modelFactory, $userHelper, $coreParametersHelper, $dispatcher, $translator, $flashBag, $requestStack, $security);
     }
 
     /**
diff --git a/Controller/CompanySegmentController.php b/Controller/CompanySegmentController.php
index 1fea616..9346610 100644
--- a/Controller/CompanySegmentController.php
+++ b/Controller/CompanySegmentController.php
@@ -7,7 +7,6 @@
 use Doctrine\ORM\EntityNotFoundException;
 use Doctrine\Persistence\ManagerRegistry;
 use Mautic\CoreBundle\Controller\AbstractStandardFormController;
-use Mautic\CoreBundle\Factory\MauticFactory;
 use Mautic\CoreBundle\Factory\ModelFactory;
 use Mautic\CoreBundle\Helper\CoreParametersHelper;
 use Mautic\CoreBundle\Helper\UserHelper;
@@ -20,6 +19,7 @@
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Stat\SegmentDependencies;
 use Symfony\Component\EventDispatcher\EventDispatcherInterface;
 use Symfony\Component\Form\FormFactoryInterface;
+use Symfony\Component\HttpFoundation\JsonResponse;
 use Symfony\Component\HttpFoundation\Request;
 use Symfony\Component\HttpFoundation\RequestStack;
 use Symfony\Component\HttpFoundation\Response;
@@ -34,7 +34,6 @@ public function __construct(
         FormFactoryInterface $formFactory,
         FormFieldHelper $fieldHelper,
         ManagerRegistry $managerRegistry,
-        MauticFactory $factory,
         ModelFactory $modelFactory,
         UserHelper $userHelper,
         CoreParametersHelper $coreParametersHelper,
@@ -45,7 +44,7 @@ public function __construct(
         CorePermissions $security,
         private SegmentDependencies $segmentDependencies,
     ) {
-        parent::__construct($formFactory, $fieldHelper, $managerRegistry, $factory, $modelFactory, $userHelper, $coreParametersHelper, $dispatcher, $translator, $flashBag, $requestStack, $security);
+        parent::__construct($formFactory, $fieldHelper, $managerRegistry, $modelFactory, $userHelper, $coreParametersHelper, $dispatcher, $translator, $flashBag, $requestStack, $security);
     }
 
     /**
@@ -185,7 +184,7 @@ private function updateCountCompaniesCache(array $companySegmentIds): void
     }
 
     /**
-     * @return Response|array<string, mixed>
+     * @return Response|JsonResponse|\Symfony\Component\HttpFoundation\RedirectResponse|array<string, mixed>
      */
     public function newAction(Request $request): Response|array
     {
diff --git a/Entity/CompaniesSegmentsRepository.php b/Entity/CompaniesSegmentsRepository.php
index 505ec2a..63c9083 100644
--- a/Entity/CompaniesSegmentsRepository.php
+++ b/Entity/CompaniesSegmentsRepository.php
@@ -24,11 +24,11 @@ public function getCompanyCount(array $segmentIds): array
     {
         $q = $this->getEntityManager()->getConnection()->createQueryBuilder();
         $q->select('COUNT(1) as thecount, '.CompaniesSegments::RELATIONS_NAME.'.segment_id')
-            ->from(MAUTIC_TABLE_PREFIX.CompaniesSegments::TABLE_NAME, CompaniesSegments::RELATIONS_NAME)
+            ->from($this->getPreTable().CompaniesSegments::TABLE_NAME, CompaniesSegments::RELATIONS_NAME)
             ->groupBy(CompaniesSegments::RELATIONS_NAME.'.segment_id');
 
         if (1 === count($segmentIds)) {
-            $q          = $this->forceUseIndex($q, MAUTIC_TABLE_PREFIX.'companies_segment_manually_removed');
+            $q          = $this->forceUseIndex($q, $this->getPreTable().'companies_segment_manually_removed');
             $expression = $q->expr()->eq(CompaniesSegments::RELATIONS_NAME.'.segment_id', (string) array_pop($segmentIds));
         } else {
             $expression = $q->expr()->in(CompaniesSegments::RELATIONS_NAME.'.segment_id', array_map(static function ($segmentId): string {
@@ -62,6 +62,16 @@ private function forceUseIndex(QueryBuilder $qb, string $indexName): QueryBuilde
     {
         $fromPart = $qb->getQueryPart('from');
         \assert(is_array($fromPart));
+        if (
+            !array_key_exists(0, $fromPart)
+            || !is_array($fromPart[0])
+            || !array_key_exists('alias', $fromPart[0])
+            || !array_key_exists('table', $fromPart[0])
+            || !is_string($fromPart[0]['alias'])
+            || !is_string($fromPart[0]['table'])
+        ) {
+            return $qb;
+        }
 
         $fromPart[0]['alias'] = sprintf('%s USE INDEX (%s)', $fromPart[0]['alias'], $indexName);
         $qb->resetQueryPart('from');
@@ -84,4 +94,13 @@ public function getCompaniesSegmentsBySegmentIds(array $segmentIds): array
 
         return array_values(array_unique($result, SORT_REGULAR));
     }
+
+    private function getPreTable(): string
+    {
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            return MAUTIC_TABLE_PREFIX;
+        }
+
+        return '';
+    }
 }
diff --git a/EventListener/ReportSubscriber.php b/EventListener/ReportSubscriber.php
index d9839c3..38fa37d 100644
--- a/EventListener/ReportSubscriber.php
+++ b/EventListener/ReportSubscriber.php
@@ -92,14 +92,14 @@ public function onReportBuilder(ReportBuilderEvent $event): void
     }
 
     /**
-     * @return array<string, string>
+     * @return array<int|string, string>
      */
     public function getFilterSegments(): array
     {
         $segments    = $this->companySegmentRepository->getSegmentObjectsViaListOfIDs();
+        /** @var array<string, string> $segmentList */
         $segmentList = [];
         foreach ($segments as $segment) {
-            /** @var string $id */
             $id               = (string) $segment->getId();
             $segmentList[$id] = (string) $segment->getName();
         }
@@ -118,18 +118,32 @@ public function onReportGenerate(ReportGeneratorEvent $event): void
 
         $qb       = $event->getQueryBuilder();
         $filters  = $event->getReport()->getFilters();
+        /** @var array<string, array<string, string>> $options */
         $options  = $event->getOptions()['columns'];
+        /** @var array<int, CompositeExpression|string> $orGroups */
         $orGroups = [];
+        /** @var array<int, CompositeExpression|string> $andGroup */
         $andGroup = [];
 
         $qb
-            ->from(MAUTIC_TABLE_PREFIX.self::COMPANY_TABLE, self::COMPANIES_PREFIX);
+            ->from($this->getBaseTableName().self::COMPANY_TABLE, self::COMPANIES_PREFIX);
 
         $expr     = $qb->expr();
 
         if (count($filters) > 0) {
             foreach ($filters as $i => $filter) {
+                /** @var array<string,mixed> $filter */
+                if (!is_array($filter)) {
+                    continue;
+                }
+                if (!array_key_exists('expr', $filter) && !array_key_exists('condition', $filter) && !array_key_exists('column', $filter)) {
+                    continue;
+                }
+                if (!is_string($filter['column'])) {
+                    continue;
+                }
                 $exprFunction = $filter['expr'] ?? $filter['condition'];
+
                 $paramName    = sprintf('i%dc%s', $i, InputHelper::alphanum($filter['column']));
 
                 if (array_key_exists('glue', $filter) && 'or' === $filter['glue']) {
@@ -172,7 +186,7 @@ public function onReportGenerate(ReportGeneratorEvent $event): void
                         $andGroup[] = $expression;
                         break;
                     default:
-                        if ('' == trim($filter['value'])) {
+                        if (!is_string($filter['value']) || '' === trim($filter['value'])) {
                             // Ignore empty
                             break;
                         }
@@ -249,7 +263,7 @@ public function onReportGenerate(ReportGeneratorEvent $event): void
     }
 
     /**
-     * @param array<string, string|null> $filter
+     * @param array<string, mixed> $filter
      */
     public function getCompanySegmentCondition(array $filter): ?string
     {
@@ -263,7 +277,7 @@ public function getCompanySegmentCondition(array $filter): ?string
     }
 
     /**
-     * @param array<string, string|null> $filter
+     * @param array<string, mixed> $filter
      */
     private function checkIfCompanySegmentFilter(array $filter): bool
     {
@@ -273,11 +287,11 @@ private function checkIfCompanySegmentFilter(array $filter): bool
     private function prepareSegmentSubQuery(): QueryBuilder
     {
         return $this->db->createQueryBuilder()->select('DISTINCT '.self::COMPANY_SEGMENTS_XREF_PREFIX.'.company_id')
-            ->from(MAUTIC_TABLE_PREFIX.self::COMPANY_SEGMENTS_XREF_TABLE, self::COMPANY_SEGMENTS_XREF_PREFIX);
+            ->from($this->getBaseTableName().self::COMPANY_SEGMENTS_XREF_TABLE, self::COMPANY_SEGMENTS_XREF_PREFIX);
     }
 
     /**
-     * @param array<string, string|null> $filter
+     * @param array<string, mixed> $filter
      */
     private function finalizeSubQuery(QueryBuilder $segmentSubQuery, array $filter): string
     {
@@ -304,8 +318,20 @@ private function finalizeSubQuery(QueryBuilder $segmentSubQuery, array $filter):
      */
     private function doesColumnSupportEmptyValue(array $filter, array $filterDefinitions): bool
     {
+        if (!array_key_exists('column', $filter) || !is_string($filter['column'])) {
+            return false;
+        }
         $type = $filterDefinitions[$filter['column']]['type'] ?? null;
 
         return !in_array($type, ['date', 'datetime'], true);
     }
+
+    private function getBaseTableName(): string
+    {
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            return MAUTIC_TABLE_PREFIX;
+        }
+
+        return '';
+    }
 }
diff --git a/EventListener/TypeOperatorSubscriber.php b/EventListener/TypeOperatorSubscriber.php
index 636791b..a0b6d50 100644
--- a/EventListener/TypeOperatorSubscriber.php
+++ b/EventListener/TypeOperatorSubscriber.php
@@ -133,9 +133,17 @@ public function updateGenerateSegmentFiltersAddCustomFieldsToLeadSegment(LeadLis
             $properties['type'] = $type;
 
             if ('boolean' === $type) {
+                $no  = 'no';
+                $yes = 'yes';
+                if (array_key_exists('no', $properties) && is_string($properties['no'])) {
+                    $no = $properties['no'];
+                }
+                if (array_key_exists('yes', $properties) && is_string($properties['yes'])) {
+                    $yes = $properties['yes'];
+                }
                 $properties['list'] = [
-                    $properties['no']  => 0,
-                    $properties['yes'] => 1,
+                    $no  => 0,
+                    $yes => 1,
                 ];
             } elseif (in_array($type, ['select', 'multiselect'], true)) {
                 $properties['list'] = FormFieldHelper::parseListForChoices($properties['list'] ?? []);
@@ -170,9 +178,17 @@ public function onGenerateSegmentFiltersAddCustomFields(CompanySegmentFiltersCho
             $properties['type'] = $type;
 
             if ('boolean' === $type) {
+                $no  = 'no';
+                $yes = 'yes';
+                if (array_key_exists('no', $properties) && is_string($properties['no'])) {
+                    $no = $properties['no'];
+                }
+                if (array_key_exists('yes', $properties) && is_string($properties['yes'])) {
+                    $yes = $properties['yes'];
+                }
                 $properties['list'] = [
-                    $properties['no']  => 0,
-                    $properties['yes'] => 1,
+                    $no  => 0,
+                    $yes => 1,
                 ];
             } elseif (in_array($type, ['select', 'multiselect'], true)) {
                 $properties['list'] = FormFieldHelper::parseListForChoices($properties['list'] ?? []);
@@ -241,6 +257,7 @@ private function setIncludeExcludeOperatorsToTextFiltersToCompanySegment(Company
     {
         $choices = $event->getChoices();
         $choices = $this->setIncludeExcludeOperatorsToTextFilters($choices);
+        // @phpstan-ignore-next-line
         $event->setChoices($choices);
     }
 
@@ -257,32 +274,53 @@ private function setIncludeExcludeOperatorsToTextFiltersToLeadSegment(LeadListFi
     }
 
     /**
-     * @param array <string, array<string, array<string, mixed>>> $choices
-     * @param array <int,string>                                  $groupAllow
+     * Normalize and validate incoming choices to ensure string keys and array values,
+     * then add include/exclude operators to text fields.
      *
-     * @return array <string, array<string, array<string, mixed>>>
+     * @param array<mixed>      $choices
+     * @param array<int,string> $groupAllow
+     *
+     * @return array<mixed>
      */
-    private function setIncludeExcludeOperatorsToTextFilters(array $choices, array $groupAllow =[]): array
+    private function setIncludeExcludeOperatorsToTextFilters(array $choices, array $groupAllow = []): array
     {
+        // Normalize to array<string, array<string, array<string,mixed>>>
+        $normalized = [];
+
         foreach ($choices as $group => $groups) {
-            if ([] !== $groupAllow && !in_array($group, $groupAllow, true)) {
+            if (!is_string($group) || !is_array($groups)) {
                 continue;
             }
-            if (!is_array($groups)) {
+
+            foreach ($groups as $alias => $choice) {
+                if (!is_string($alias) || !is_array($choice)) {
+                    continue;
+                }
+
+                $normalized[$group][$alias] = $choice;
+            }
+        }
+
+        foreach ($normalized as $group => $groups) {
+            if ([] !== $groupAllow && !in_array($group, $groupAllow, true)) {
                 continue;
             }
+
             foreach ($groups as $alias => $choice) {
                 $type = null;
-                if (is_array($choice) && is_array($choice['properties'])) {
+                if (isset($choice['properties']) && is_array($choice['properties'])) {
                     $type = $choice['properties']['type'] ?? null;
                 }
+
                 if ('text' === $type) {
-                    assert(is_array($choices[$group]));
-                    if (!is_array($choices[$group][$alias])) {
-                        $choices[$group][$alias] = [];
+                    if (!array_key_exists($group, $normalized) || !is_array($normalized[$group])) {
+                        $normalized[$group] = [];
+                    }
+                    if (!isset($normalized[$group][$alias]) || !is_array($normalized[$group][$alias])) {
+                        $normalized[$group][$alias] = [];
                     }
 
-                    $choices[$group][$alias]['operators'] = $this->typeOperatorProvider->getOperatorsIncluding([
+                    $normalized[$group][$alias]['operators'] = $this->typeOperatorProvider->getOperatorsIncluding([
                         OperatorOptions::EQUAL_TO,
                         OperatorOptions::NOT_EQUAL_TO,
                         OperatorOptions::EMPTY,
@@ -301,6 +339,6 @@ private function setIncludeExcludeOperatorsToTextFilters(array $choices, array $
             }
         }
 
-        return $choices;
+        return $normalized;
     }
 }
diff --git a/EventListener/UpdatePluginSubscriber.php b/EventListener/UpdatePluginSubscriber.php
index e592aa5..abea22d 100644
--- a/EventListener/UpdatePluginSubscriber.php
+++ b/EventListener/UpdatePluginSubscriber.php
@@ -4,8 +4,7 @@
 
 namespace MauticPlugin\LeuchtfeuerCompanySegmentsBundle\EventListener;
 
-use Mautic\CoreBundle\Factory\MauticFactory;
-use Mautic\PluginBundle\Bundle\PluginBundleBase;
+use Mautic\PluginBundle\Bundle\PluginDatabase;
 use Mautic\PluginBundle\Event\PluginUpdateEvent;
 use Mautic\PluginBundle\Model\PluginModel;
 use Mautic\PluginBundle\PluginEvents;
@@ -15,7 +14,7 @@ class UpdatePluginSubscriber implements EventSubscriberInterface
 {
     public function __construct(
         private PluginModel $pluginModel,
-        private MauticFactory $factory,
+        private PluginDatabase $pluginDatabase,
     ) {
         // Constructor logic if needed
     }
@@ -35,7 +34,7 @@ public function onPluginUpdate(PluginUpdateEvent $event): void
         $tableInstalled          =$this->pluginModel->getInstalledPluginTables($pluginMetadata);
         if ([] === $tableInstalled) {
             // No tables installed, so we can install the schema
-            PluginBundleBase::installPluginSchema($companyEventLogMetadata, $this->factory);
+            $this->pluginDatabase->installPluginSchema($companyEventLogMetadata);
 
             return;
         }
@@ -49,7 +48,8 @@ public function onPluginUpdate(PluginUpdateEvent $event): void
                 }
             }
         }
+
         // work here to check if schema is already installed
-        PluginBundleBase::installPluginSchema($companyEventLogMetadata, $this->factory);
+        $this->pluginDatabase->installPluginSchema($companyEventLogMetadata);
     }
 }
diff --git a/Model/CompanySegmentActionModel.php b/Model/CompanySegmentActionModel.php
index e9fd5fe..734589f 100644
--- a/Model/CompanySegmentActionModel.php
+++ b/Model/CompanySegmentActionModel.php
@@ -18,8 +18,8 @@ public function __construct(
     }
 
     /**
-     * @param array<int> $companyIds
-     * @param array<int> $segmentIds
+     * @param array<mixed,mixed> $companyIds
+     * @param array<mixed,mixed> $segmentIds
      */
     public function addCompanies(array $companyIds, array $segmentIds, bool $manuallyAdded = true): void
     {
@@ -35,8 +35,8 @@ public function addCompanies(array $companyIds, array $segmentIds, bool $manuall
     }
 
     /**
-     * @param array<int> $companyIds
-     * @param array<int> $segmentIds
+     * @param array<mixed,mixed> $companyIds
+     * @param array<mixed,mixed> $segmentIds
      */
     public function removeCompanies(array $companyIds, array $segmentIds, bool $manuallyRemoved = true): void
     {
diff --git a/Model/CompanySegmentModel.php b/Model/CompanySegmentModel.php
index 2a48cf3..c3fdee7 100644
--- a/Model/CompanySegmentModel.php
+++ b/Model/CompanySegmentModel.php
@@ -16,6 +16,7 @@
 use Mautic\CoreBundle\Translation\Translator;
 use Mautic\LeadBundle\Entity\Company;
 use Mautic\LeadBundle\Entity\CompanyRepository;
+use Mautic\LeadBundle\Entity\LeadList;
 use Mautic\LeadBundle\Entity\OperatorListTrait;
 use Mautic\LeadBundle\Model\ListModel;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Entity\CompaniesSegments;
@@ -116,8 +117,12 @@ public function saveEntity($entity, $unlock = true): void
 
         // set some defaults
         $this->setTimestamps($entity, $isNew, $unlock);
-
+        assert($entity instanceof CompanySegment);
         $alias = $entity->getAlias();
+        if (is_null($alias)) {
+            $alias = '';
+        }
+
         $alias = $this->cleanAlias($alias, '', 0, '-');
 
         // make sure alias is not already taken
@@ -164,8 +169,40 @@ public function getChoiceFields(): array
         }
 
         // Order choices by label.
+        /** @var array<string, array<int, array<string,mixed>>> $choices */
         foreach ($choices as $key => $choice) {
-            $cmp = static fn ($a, $b): int => strcmp($a['label'], $b['label']);
+            if (!is_array($choice)) {
+                // skip invalid choice set
+                continue;
+            }
+
+            $getLabel = static function ($item): string {
+                if (is_array($item) && array_key_exists('label', $item)) {
+                    $label = $item['label'];
+                    if (is_string($label)) {
+                        return $label;
+                    }
+                    if (is_numeric($label) || is_bool($label)) {
+                        return (string) $label;
+                    }
+
+                    return '';
+                }
+
+                if (is_string($item)) {
+                    return $item;
+                }
+
+                if (is_numeric($item) || is_bool($item)) {
+                    return (string) $item;
+                }
+
+                return '';
+            };
+            $cmp = static function ($a, $b) use ($getLabel): int {
+                return strcmp($getLabel($a), $getLabel($b));
+            };
+
             uasort($choice, $cmp);
             $choices[$key] = $choice;
         }
@@ -307,13 +344,14 @@ public function getSegmentsWithDependenciesOnSegment(int $segmentId, string $ret
         $dependents = [];
         $accessor   = PropertyAccess::createPropertyAccessor();
         foreach ($entities as $entity) {
+            assert($entity instanceof CompanySegment || $entity instanceof LeadList);
             $retrFilters = $entity->getFilters();
             foreach ($retrFilters as $eachFilter) {
-                if (!array_key_exists('properties', $eachFilter) || !array_key_exists('filter', $eachFilter['properties'])) {
+                if (!is_array($eachFilter) || !array_key_exists('properties', $eachFilter) || !is_array($eachFilter['properties']) || !array_key_exists('filter', $eachFilter['properties'])) {
                     continue;
                 }
                 $filter = $eachFilter['properties']['filter'];
-                if ($filter && self::PROPERTIES_FIELD === $eachFilter['type'] && in_array($segmentId, $filter, true)) {
+                if (is_array($filter) && self::PROPERTIES_FIELD === $eachFilter['type'] && in_array($segmentId, $filter, true)) {
                     $value = $accessor->getValue($entity, $returnProperty);
                     if (('id' !== $returnProperty && !is_string($value)) || ('id' === $returnProperty && !is_numeric($value))) {
                         continue; // Return property does not exist.
@@ -359,9 +397,16 @@ public function canNotBeDeletedByCompanySegments(array $segmentIds): array
         $filterRegistered          = [];
 
         foreach ($entities as $entity) {
+            assert($entity instanceof CompanySegment);
             $retrFilters = $entity->getFilters();
             foreach ($retrFilters as $eachFilter) {
-                if (self::PROPERTIES_FIELD !== $eachFilter['type']) {
+                if (
+                    !is_array($eachFilter)
+                    || !array_key_exists('type', $eachFilter)
+                    || self::PROPERTIES_FIELD !== $eachFilter['type']
+                    || !is_array($eachFilter['properties'])
+                    || !array_key_exists('filter', $eachFilter['properties'])
+                ) {
                     continue;
                 }
                 /** @var array<int> $filterValue */
@@ -443,10 +488,16 @@ public function canNotBeDeletedByLeadSegments(array $segmentIds): array
         $filterRegistered    = [];
 
         foreach ($entities as $entity) {
-            assert($entity instanceof \Mautic\LeadBundle\Entity\LeadList);
+            assert($entity instanceof LeadList);
             $retrFilters = $entity->getFilters();
             foreach ($retrFilters as $eachFilter) {
-                if (self::PROPERTIES_FIELD !== $eachFilter['type']) {
+                if (
+                    !is_array($eachFilter)
+                    || !array_key_exists('type', $eachFilter)
+                    || self::PROPERTIES_FIELD !== $eachFilter['type']
+                    || !is_array($eachFilter['properties'])
+                    || !array_key_exists('filter', $eachFilter['properties'])
+                ) {
                     continue;
                 }
                 /** @var array<int> $filterValue */
@@ -522,11 +573,19 @@ public function canNotBeDeleted(array $segmentIds, bool $isContactSegment = fals
         $filterRegistered          = [];
 
         foreach ($entities as $entity) {
+            assert($entity instanceof CompanySegment || $entity instanceof LeadList);
             $retrFilters = $entity->getFilters();
             foreach ($retrFilters as $eachFilter) {
-                if (self::PROPERTIES_FIELD !== $eachFilter['type']) {
+                if (
+                    !is_array($eachFilter)
+                    || !array_key_exists('type', $eachFilter)
+                    || self::PROPERTIES_FIELD !== $eachFilter['type']
+                    || !is_array($eachFilter['properties'])
+                    || !array_key_exists('filter', $eachFilter['properties'])
+                ) {
                     continue;
                 }
+
                 if (!array_key_exists('properties', $eachFilter) || !array_key_exists('filter', $eachFilter['properties'])) {
                     continue;
                 }
@@ -622,6 +681,7 @@ public function addCompany(Company $company, iterable $companySegments, bool $ma
 
         $companyAddSegment = [];
         foreach ($companySegments as $companySegment) {
+            assert($companySegment instanceof CompanySegment);
             if ($companySegment->hasCompany($company)) {
                 continue;
             }
@@ -645,6 +705,7 @@ public function addCompany(Company $company, iterable $companySegments, bool $ma
                 }
             } else {
                 $companiesSegments = new CompaniesSegments();
+
                 $companiesSegments->setCompanySegment($companySegment);
                 $companiesSegments->setCompany($company);
                 $companiesSegments->setManuallyAdded($manuallyAdded);
@@ -654,7 +715,9 @@ public function addCompany(Company $company, iterable $companySegments, bool $ma
             $companySegment->addCompaniesSegment($companiesSegments);
 
             $companyAddSegment[] = $companiesSegments;
-            $this->segmentCountCacheHelper->incrementSegmentCompanyCount($companySegment->getId());
+            if (is_int($companySegment->getId())) {
+                $this->segmentCountCacheHelper->incrementSegmentCompanyCount($companySegment->getId());
+            }
         }
 
         foreach ($companyAddSegment as $companiesSegment) {
@@ -700,6 +763,7 @@ public function removeCompany(Company $company, iterable $companySegments, bool
         $companySaveSegment   = [];
         $companyDeleteSegment = [];
         foreach ($companySegments as $companySegment) {
+            assert($companySegment instanceof CompanySegment);
             $companiesSegments = $this->getCompaniesSegmentsRepository()->findOneBy(
                 [
                     'company'        => $company,
@@ -720,8 +784,9 @@ public function removeCompany(Company $company, iterable $companySegments, bool
 
                 $companySaveSegment[$companySegment->getId()] = $companiesSegments;
             }
-
-            $this->segmentCountCacheHelper->decrementSegmentCompanyCount($companySegment->getId());
+            if (is_int($companySegment->getId())) {
+                $this->segmentCountCacheHelper->decrementSegmentCompanyCount($companySegment->getId());
+            }
         }
 
         if ([] !== $companySaveSegment) {
@@ -836,7 +901,7 @@ public function rebuildCompanySegment(CompanySegment $companySegment, int $limit
                     $processedCompanies[] = $company;
 
                     ++$companiesProcessed;
-                    if (null !== $output && $companiesProcessed < $maxCount) {
+                    if (null !== $output && $companiesProcessed < $maxCount && isset($progress)) {
                         $progress->setProgress($companiesProcessed);
                     }
 
diff --git a/Segment/Query/CompanySegmentQueryBuilder.php b/Segment/Query/CompanySegmentQueryBuilder.php
index fdfbad1..73ed37f 100644
--- a/Segment/Query/CompanySegmentQueryBuilder.php
+++ b/Segment/Query/CompanySegmentQueryBuilder.php
@@ -55,7 +55,7 @@ public function assembleCompaniesSegmentQueryBuilder(CompanySegment $companySegm
 
         $companyTableAlias        = $changeAlias ? $this->generateRandomParameterName() : $this->companyRepository->getTableAlias();
 
-        $queryBuilder->select($companyTableAlias.'.id')->from(MAUTIC_TABLE_PREFIX.'companies', $companyTableAlias);
+        $queryBuilder->select($companyTableAlias.'.id')->from($this->getBaseTableName().'companies', $companyTableAlias);
         /*
          * Validate the plan, check for circular dependencies.
          *
@@ -119,20 +119,20 @@ public function assembleCompaniesSegmentQueryBuilderLeadSegment(CompanySegment $
         $leadTableAlias           = $this->generateRandomParameterName();
         $companyLeadsTableAlias   = $this->generateRandomParameterName();
         $companySegmentTableAlias = $this->generateRandomParameterName();
-        $queryBuilder->select($leadTableAlias.'.id')->from(MAUTIC_TABLE_PREFIX.'leads', $leadTableAlias)
+        $queryBuilder->select($leadTableAlias.'.id')->from($this->getBaseTableName().'leads', $leadTableAlias)
             ->join(
                 $leadTableAlias,
-                MAUTIC_TABLE_PREFIX.'companies_leads',
+                $this->getBaseTableName().'companies_leads',
                 $companyLeadsTableAlias,
                 $companyLeadsTableAlias.'.lead_id = '.$leadTableAlias.'.id and '.$companyLeadsTableAlias.'.is_primary = 1'
             )->join(
                 $companyLeadsTableAlias,
-                MAUTIC_TABLE_PREFIX.'companies',
+                $this->getBaseTableName().'companies',
                 $companyTableAlias,
                 $companyTableAlias.'.id = '.$companyLeadsTableAlias.'.company_id'
             )->join(
                 $companyTableAlias,
-                MAUTIC_TABLE_PREFIX.'companies_segments',
+                $this->getBaseTableName().'companies_segments',
                 $companySegmentTableAlias,
                 $companySegmentTableAlias.'.segment_id = '.$companySegment->getId().' and '.$companySegmentTableAlias.'.manually_removed = 0',
             );
@@ -219,7 +219,7 @@ public function wrapInCount(QueryBuilder $qb): QueryBuilder
      */
     public function addNewCompaniesRestrictions(QueryBuilder $queryBuilder, CompanySegment $segment, array $batchLimiters = []): QueryBuilder
     {
-        $companiesTableAlias = $queryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+        $companiesTableAlias = $queryBuilder->getTableAlias($this->getBaseTableName().'companies');
         \assert(is_string($companiesTableAlias));
         $expr               = $queryBuilder->expr();
         $tableAlias         = $this->generateRandomParameterName();
@@ -231,7 +231,7 @@ public function addNewCompaniesRestrictions(QueryBuilder $queryBuilder, CompanyS
 
         $segmentQueryBuilder = $queryBuilder->createQueryBuilder()
             ->select($tableAlias.'.company_id')
-            ->from(MAUTIC_TABLE_PREFIX.CompaniesSegments::TABLE_NAME, $tableAlias)
+            ->from($this->getBaseTableName().CompaniesSegments::TABLE_NAME, $tableAlias)
             ->andWhere($expr->eq($tableAlias.'.segment_id', $segmentIdParameter));
 
         $queryBuilder->setParameter(sprintf('%ssegmentId', $tableAlias), $segmentId);
@@ -245,7 +245,7 @@ public function addNewCompaniesRestrictions(QueryBuilder $queryBuilder, CompanyS
 
     public function addCompanySegmentQuery(QueryBuilder $queryBuilder, CompanySegment $companySegment): QueryBuilder
     {
-        $companiesTableAlias = $queryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+        $companiesTableAlias = $queryBuilder->getTableAlias($this->getBaseTableName().'companies');
         \assert(is_string($companiesTableAlias));
         $tableAlias = $this->generateRandomParameterName();
 
@@ -253,7 +253,7 @@ public function addCompanySegmentQuery(QueryBuilder $queryBuilder, CompanySegmen
 
         $existsQueryBuilder
             ->select('null')
-            ->from(MAUTIC_TABLE_PREFIX.CompaniesSegments::TABLE_NAME, $tableAlias)
+            ->from($this->getBaseTableName().CompaniesSegments::TABLE_NAME, $tableAlias)
             ->andWhere($queryBuilder->expr()->eq($tableAlias.'.segment_id', (int) $companySegment->getId()));
 
         $existingQueryWherePart = $existsQueryBuilder->getQueryPart('where');
@@ -280,10 +280,10 @@ public function queryBuilderGenerated(CompanySegment $companySegment, QueryBuild
     public function addManuallySubscribedQuery(QueryBuilder $queryBuilder, CompanySegment $companySegment): QueryBuilder
     {
         \assert(null !== $companySegment->getId());
-        $companyTableAlias = $queryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+        $companyTableAlias = $queryBuilder->getTableAlias($this->getBaseTableName().'companies');
 
         if (!is_string($companyTableAlias)) {
-            throw new \LogicException('The table alias for '.MAUTIC_TABLE_PREFIX.'companies must be a string.');
+            throw new \LogicException('The table alias for '.$this->getBaseTableName().'companies must be a string.');
         }
 
         $tableAlias = $this->generateRandomParameterName();
@@ -292,7 +292,7 @@ public function addManuallySubscribedQuery(QueryBuilder $queryBuilder, CompanySe
 
         $existsQueryBuilder
             ->select('null')
-            ->from(MAUTIC_TABLE_PREFIX.CompaniesSegments::TABLE_NAME, $tableAlias)
+            ->from($this->getBaseTableName().CompaniesSegments::TABLE_NAME, $tableAlias)
             ->andWhere($queryBuilder->expr()->eq($tableAlias.'.segment_id', $companySegment->getId()))
             ->andWhere(
                 $queryBuilder->expr()->or(
@@ -320,16 +320,16 @@ public function addManuallySubscribedQuery(QueryBuilder $queryBuilder, CompanySe
     public function addManuallyUnsubscribedQuery(QueryBuilder $queryBuilder, CompanySegment $companySegment): QueryBuilder
     {
         \assert(null !== $companySegment->getId());
-        $companyTableAlias = $queryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+        $companyTableAlias = $queryBuilder->getTableAlias($this->getBaseTableName().'companies');
 
         if (!is_string($companyTableAlias)) {
-            throw new \LogicException('The table alias for '.MAUTIC_TABLE_PREFIX.'companies must be a string.');
+            throw new \LogicException('The table alias for '.$this->getBaseTableName().'companies must be a string.');
         }
 
         $tableAlias = $this->generateRandomParameterName();
         $queryBuilder->leftJoin(
             $companyTableAlias,
-            MAUTIC_TABLE_PREFIX.CompaniesSegments::TABLE_NAME,
+            $this->getBaseTableName().CompaniesSegments::TABLE_NAME,
             $tableAlias,
             $companyTableAlias.'.id = '.$tableAlias.'.company_id and '.$tableAlias.'.segment_id = :manually_unsubscribed_segment_id'
         )->setParameter('manually_unsubscribed_segment_id', $companySegment->getId())
@@ -428,4 +428,13 @@ private function getSegmentEdges(CompanySegment $companySegment): array
 
         return $segmentEdges;
     }
+
+    private function getBaseTableName(): string
+    {
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            return MAUTIC_TABLE_PREFIX;
+        }
+
+        return '';
+    }
 }
diff --git a/Segment/Query/Filter/SegmentReferenceFilterQueryBuilder.php b/Segment/Query/Filter/SegmentReferenceFilterQueryBuilder.php
index 6962e05..f703490 100644
--- a/Segment/Query/Filter/SegmentReferenceFilterQueryBuilder.php
+++ b/Segment/Query/Filter/SegmentReferenceFilterQueryBuilder.php
@@ -57,10 +57,12 @@ public function applyQuery(QueryBuilder $queryBuilder, ContactSegmentFilter $fil
 
         $from = $queryBuilder->getQueryPart('from');
         assert(is_array($from));
+
         if (
             array_key_exists(0, $from)
+            && is_array($from[0])
             && array_key_exists('table', $from[0])
-            && $from[0]['table'] === MAUTIC_TABLE_PREFIX.'leads'
+            && $from[0]['table'] === $this->getPreTable().'leads'
         ) {
             return $this->applyQueryToLeadSegment($queryBuilder, $filter);
         }
@@ -70,7 +72,7 @@ public function applyQuery(QueryBuilder $queryBuilder, ContactSegmentFilter $fil
 
     private function applyQueryToCompanySegment(QueryBuilder $queryBuilder, ContactSegmentFilter $filter): QueryBuilder
     {
-        $companiesTableAlias = $queryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+        $companiesTableAlias = $queryBuilder->getTableAlias($this->getPreTable().'companies');
         \assert(is_string($companiesTableAlias));
         $segmentIds = $filter->getParameterValue();
         if (OperatorOptions::EMPTY === $filter->getOperator() || 'notEmpty' === $filter->getOperator()) {
@@ -81,13 +83,16 @@ private function applyQueryToCompanySegment(QueryBuilder $queryBuilder, ContactS
                 && is_array($dataArray['properties'])
                 && array_key_exists('current_company_id', $dataArray['properties'])
             ) {
-                $currentSegmentId = (int) $dataArray['properties']['current_company_id'];
+                $raw = $dataArray['properties']['current_company_id'];
+                if (is_int($raw) || (is_string($raw) && ctype_digit($raw)) || is_numeric($raw)) {
+                    $currentSegmentId = (int) $raw;
+                }
             }
 
             $sub  = $queryBuilder->createQueryBuilder();
             $t    = $this->generateRandomParameterName();
             $sub->select('1')
-                ->from(MAUTIC_TABLE_PREFIX.'companies_segments', $t)
+                ->from($this->getPreTable().'companies_segments', $t)
                 ->where($sub->expr()->eq($t.'.company_id', $companiesTableAlias.'.id'));
 
             // exclude current segment id if provided
@@ -117,14 +122,14 @@ private function applyQueryToCompanySegment(QueryBuilder $queryBuilder, ContactS
             /** @var CompanySegment|null $companySegment */
             $companySegment = $this->entityManager->getRepository(CompanySegment::class)->find($segmentId);
             if (null === $companySegment) {
-                throw new SegmentNotFoundException(sprintf('Segment %d used in the filter does not exist anymore.', $segmentId));
+                throw new SegmentNotFoundException(sprintf('Segment %s used in the filter does not exist anymore.', $this->stringify($segmentId)));
             }
 
             $contactSegment = new CompanySegmentAsLeadSegment($companySegment);
             $filters        = $this->leadSegmentFilterFactory->getSegmentFilters($contactSegment);
 
             $segmentQueryBuilder           = $this->companySegmentQueryBuilder->assembleCompaniesSegmentQueryBuilder($companySegment, $filters, true);
-            $subSegmentCompaniesTableAlias = $segmentQueryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+            $subSegmentCompaniesTableAlias = $segmentQueryBuilder->getTableAlias($this->getPreTable().'companies');
             \assert(is_string($subSegmentCompaniesTableAlias));
             $segmentQueryBuilder->resetQueryParts(['select'])->select('null');
 
@@ -175,12 +180,12 @@ private function applyQueryToCompanySegment(QueryBuilder $queryBuilder, ContactS
 
     private function applyQueryToLeadSegment(QueryBuilder $queryBuilder, ContactSegmentFilter $filter): QueryBuilder
     {
-        $leadAlias               = $queryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'leads');
+        $leadAlias               = $queryBuilder->getTableAlias($this->getPreTable().'leads');
         $companiesLeadTableAlias = $this->generateRandomParameterName();
         assert(is_string($leadAlias));
         $queryBuilder->join(
             $leadAlias,
-            MAUTIC_TABLE_PREFIX.'companies_leads',
+            $this->getPreTable().'companies_leads',
             $companiesLeadTableAlias,
             $companiesLeadTableAlias.'.lead_id = '.$leadAlias.'.id AND '.$companiesLeadTableAlias.'.is_primary = 1'
         );
@@ -205,7 +210,7 @@ private function applyQueryToLeadSegment(QueryBuilder $queryBuilder, ContactSegm
             $companySegment    = $this->entityManager->getRepository(CompanySegment::class)->find($segmentId);
 
             if (null === $companySegment) {
-                throw new SegmentNotFoundException(sprintf('Segment %d used in the filter does not exist anymore.', $segmentId));
+                throw new SegmentNotFoundException(sprintf('Segment %s used in the filter does not exist anymore.', $this->stringify($segmentId)));
             }
 
             $contactSegment      = new CompanySegmentAsLeadSegment($companySegment);
@@ -215,7 +220,7 @@ private function applyQueryToLeadSegment(QueryBuilder $queryBuilder, ContactSegm
                 $filters,
                 true
             );
-            $subSegmentCompaniesTableAlias = $segmentQueryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+            $subSegmentCompaniesTableAlias = $segmentQueryBuilder->getTableAlias($this->getPreTable().'companies');
             if (false === $subSegmentCompaniesTableAlias) {
                 $subSegmentCompaniesTableAlias = $this->generateRandomParameterName();
             }
@@ -278,4 +283,29 @@ public static function getSubscribedEvents(): array
             LeadEvents::SEGMENT_DICTIONARY_ON_GENERATE => 'onAddFilter',
         ];
     }
+
+    private function getPreTable(): string
+    {
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            return MAUTIC_TABLE_PREFIX;
+        }
+
+        return '';
+    }
+
+    private function stringify(mixed $value): string
+    {
+        if (is_string($value)) {
+            return $value;
+        }
+        if (is_int($value) || is_float($value) || is_bool($value)) {
+            return (string) $value;
+        }
+        if (is_object($value) && method_exists($value, '__toString')) {
+            return (string) $value;
+        }
+        $encoded = @json_encode($value);
+
+        return false !== $encoded ? $encoded : '';
+    }
 }
diff --git a/Service/CompanySegmentService.php b/Service/CompanySegmentService.php
index 91a8d52..2db48d3 100644
--- a/Service/CompanySegmentService.php
+++ b/Service/CompanySegmentService.php
@@ -54,7 +54,11 @@ public function getTotalCompanySegmentsCompaniesCount(CompanySegment $companySeg
 
         $qb = $this->companySegmentQueryBuilder->wrapInCount($qb);
 
-        $this->logger->debug('Company Segment QB: Create SQL: '.$qb->getDebugOutput(), ['segmentId' => $companySegmentId]);
+        $debug = '';
+        if (is_string($qb->getDebugOutput())) {
+            $debug = $qb->getDebugOutput();
+        }
+        $this->logger->debug('Company Segment QB: Create SQL: '.$debug, ['segmentId' => $companySegmentId]);
 
         $result = $this->timedFetch($qb, $companySegmentId);
         \assert(3 === count($result));
@@ -97,9 +101,12 @@ public function getNewCompanySegmentsCompanyCount(CompanySegment $companySegment
 
         $this->addMinMaxLimiters($qb, $batchLimiters, 'companies', 'id');
 
-        $qb = $this->companySegmentQueryBuilder->wrapInCount($qb);
-
-        $this->logger->debug('Company Segment QB: Create SQL: '.$qb->getDebugOutput(), ['segmentId' => $companySegmentId]);
+        $qb    = $this->companySegmentQueryBuilder->wrapInCount($qb);
+        $debug = '';
+        if (is_string($qb->getDebugOutput())) {
+            $debug = $qb->getDebugOutput();
+        }
+        $this->logger->debug('Company Segment QB: Create SQL: '.$debug, ['segmentId' => $companySegmentId]);
 
         $result = $this->timedFetch($qb, $companySegmentId);
 
@@ -141,7 +148,11 @@ public function getOrphanedCompanySegmentCompaniesCount(CompanySegment $segment,
 
         $segmentId = $segment->getId();
         \assert(null !== $segmentId);
-        $this->logger->debug('Company Segment QB: Orphan Companies Count SQL: '.$queryBuilder->getDebugOutput(), ['segmentId' => $segmentId]);
+        $debug = '';
+        if (is_string($queryBuilder->getDebugOutput())) {
+            $debug = $queryBuilder->getDebugOutput();
+        }
+        $this->logger->debug('Company Segment QB: Orphan Companies Count SQL: '.$debug, ['segmentId' => $segmentId]);
 
         $result = $this->timedFetch($queryBuilder, $segmentId);
 
@@ -162,7 +173,11 @@ public function getOrphanedCompanySegmentCompanies(CompanySegment $segment, arra
 
         $segmentId = $segment->getId();
         \assert(null !== $segmentId);
-        $this->logger->debug('Company Segment QB: Orphan Companies SQL: '.$queryBuilder->getDebugOutput(), ['segmentId' => $segmentId]);
+        $debug = '';
+        if (is_string($queryBuilder->getDebugOutput())) {
+            $debug = $queryBuilder->getDebugOutput();
+        }
+        $this->logger->debug('Company Segment QB: Orphan Companies SQL: '.$debug, ['segmentId' => $segmentId]);
 
         $result = $this->timedFetchAll($queryBuilder, $segmentId);
 
@@ -196,8 +211,11 @@ private function getNewCompanySegmentCompaniesQueryBuilder(CompanySegment $segme
         array_unshift($select, $distinct.$companiesTableAlias.'.id');
         $queryBuilder->resetQueryPart('select');
         $queryBuilder->select($select);
-
-        $this->logger->debug('Company Segment QB: Create Companies SQL: '.$queryBuilder->getDebugOutput(), ['segmentId' => $segment->getId()]);
+        $debug = '';
+        if (is_string($queryBuilder->getDebugOutput())) {
+            $debug = $queryBuilder->getDebugOutput();
+        }
+        $this->logger->debug('Company Segment QB: Create Companies SQL: '.$debug, ['segmentId' => $segment->getId()]);
 
         $this->addMinMaxLimiters($queryBuilder, $batchLimiters, 'companies', 'id');
 
@@ -273,7 +291,11 @@ private function getOrphanedCompanySegmentCompaniesQueryBuilder(CompanySegment $
 
         $qbO  = $queryBuilder->createQueryBuilder();
         $qbO->select('orp.company_id as id, orp.segment_id');
-        $qbO->from(MAUTIC_TABLE_PREFIX.CompaniesSegments::TABLE_NAME, 'orp');
+        $preTable = '';
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            $preTable = MAUTIC_TABLE_PREFIX;
+        }
+        $qbO->from($preTable.CompaniesSegments::TABLE_NAME, 'orp');
         $qbO->setParameters($queryBuilder->getParameters(), $queryBuilder->getParameterTypes());
         $qbO->andWhere($expr->eq('orp.segment_id', ':orpsegid'));
         $qbO->andWhere($expr->eq('orp.manually_added', ':false'));
diff --git a/Tests/Controller/AjaxControllerTest.php b/Tests/Controller/AjaxControllerTest.php
index deee7f5..f845cb9 100644
--- a/Tests/Controller/AjaxControllerTest.php
+++ b/Tests/Controller/AjaxControllerTest.php
@@ -16,12 +16,15 @@
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Entity\CompanySegment;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Helper\SegmentCountCacheHelper;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Model\CompanySegmentModel;
+use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\HelperCompanySegmentTestTrait;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\MauticMysqlTestCase;
 use Symfony\Component\HttpFoundation\Request;
 use Symfony\Component\HttpFoundation\Response;
 
 class AjaxControllerTest extends MauticMysqlTestCase
 {
+    use HelperCompanySegmentTestTrait;
+
     public function testCompanySegmentFilter(): void
     {
         $this->client->xmlHttpRequest(Request::METHOD_POST, '/s/ajax', [
@@ -189,6 +192,7 @@ public function testCheckIfCompanyCountRebuildAfterNoCache(): void
         self::assertCount(0, $companySegmentFiltered->getCompaniesSegments(), 'Check that there are no aut-saving to the cache.');
         $companySegmentDependent = $this->getCompanySegment(LoadCompanySegmentData::COMPANY_SEGMENT_DEPENDENT);
         self::assertCount(0, $companySegmentDependent->getCompaniesSegments());
+
         // Though the DB contains "proper" counts of companies in segments, the command need to be executed to fill in the cache.
         $crawler = $this->client->request(Request::METHOD_GET, '/s/company-segments');
         self::assertResponseIsSuccessful();
diff --git a/Tests/Controller/BatchSegmentControllerTest.php b/Tests/Controller/BatchSegmentControllerTest.php
index 0cca08f..84bac87 100644
--- a/Tests/Controller/BatchSegmentControllerTest.php
+++ b/Tests/Controller/BatchSegmentControllerTest.php
@@ -8,6 +8,7 @@
 use Mautic\UserBundle\DataFixtures\ORM\LoadRoleData;
 use Mautic\UserBundle\DataFixtures\ORM\LoadUserData;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\DataFixtures\ORM\LoadCompanySegmentData;
+use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\HelperCompanySegmentTestTrait;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\MauticMysqlTestCase;
 use Symfony\Component\DomCrawler\Crawler;
 use Symfony\Component\HttpFoundation\Request;
@@ -15,6 +16,8 @@
 
 class BatchSegmentControllerTest extends MauticMysqlTestCase
 {
+    use HelperCompanySegmentTestTrait;
+
     /**
      * When I add two Companies to the Company Segment “test1” via bulk action, the “# Companies” value in the Company Segments list view is increased by two.
      */
@@ -59,6 +62,7 @@ public function testCompaniesAreAddedToThenRemovedFromSegmentsInBatch(): void
         $payload = $form->getPhpValues();
         self::assertCount(1, $payload);
         self::assertArrayHasKey('company_batch', $payload);
+        self::assertIsArray($payload['company_batch']);
         self::assertCount(3, $payload['company_batch']);
         self::assertArrayHasKey('_token', $payload['company_batch']);
         self::assertArrayHasKey('add', $payload['company_batch']);
@@ -129,8 +133,10 @@ public function testCompaniesAreAddedToThenRemovedFromSegmentsInBatch(): void
             'company_batch[remove]' => [$segmentId],
         ]);
         $payload = $form->getPhpValues();
+        self::assertIsArray($payload);
         self::assertCount(1, $payload);
         self::assertArrayHasKey('company_batch', $payload);
+        self::assertIsArray($payload['company_batch']);
         self::assertCount(3, $payload['company_batch']);
         self::assertArrayHasKey('remove', $payload['company_batch']);
         self::assertArrayHasKey('_token', $payload['company_batch']);
diff --git a/Tests/Controller/CompanySegmentControllerTest.php b/Tests/Controller/CompanySegmentControllerTest.php
index 8724053..59a4e43 100644
--- a/Tests/Controller/CompanySegmentControllerTest.php
+++ b/Tests/Controller/CompanySegmentControllerTest.php
@@ -8,12 +8,14 @@
 use Mautic\UserBundle\DataFixtures\ORM\LoadUserData;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\DataFixtures\ORM\LoadCompanySegmentData;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\EnablePluginTrait;
+use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\HelperCompanySegmentTestTrait;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\MauticMysqlTestCase;
 use Symfony\Component\HttpFoundation\Request;
 
 class CompanySegmentControllerTest extends MauticMysqlTestCase
 {
     use EnablePluginTrait;
+    use HelperCompanySegmentTestTrait;
 
     protected function setUp(): void
     {
@@ -27,7 +29,7 @@ protected function setUp(): void
 
     public function testCreate(): void
     {
-        $this->loadFixtures([LoadUserData::class, LoadRoleData::class], false);
+        $this->loadFixtures([LoadUserData::class, LoadRoleData::class]);
         $segmentName = 'Segment test';
         $crawler     = $this->client->request(Request::METHOD_GET, '/s/company-segments');
         self::assertResponseIsSuccessful();
@@ -59,6 +61,7 @@ public function testCreate(): void
     public function testCreateCancel(): void
     {
         $this->loadFixtures([LoadUserData::class, LoadRoleData::class], false);
+
         $crawler     = $this->client->request(Request::METHOD_GET, '/s/company-segments');
         self::assertResponseIsSuccessful();
 
@@ -154,7 +157,7 @@ public function testDelete(): void
 
     public function testBatchDelete(): void
     {
-        $this->loadFixtures([LoadCompanySegmentData::class, LoadUserData::class, LoadRoleData::class], false);
+        $this->loadFixtures([LoadCompanySegmentData::class, LoadUserData::class, LoadRoleData::class]);
 
         $crawler = $this->client->request(Request::METHOD_GET, '/s/company-segments');
         self::assertResponseIsSuccessful();
@@ -166,16 +169,16 @@ public function testBatchDelete(): void
         self::assertCount(3, $rows);
         self::assertStringContainsString($companySegmentName, $rows->eq(1)->filter('td')->eq(1)->text());
         $link = $crawler->filter('.page-list-actions')->filter('a')->eq(0);
-        self::assertSame('Delete Selected', $link->text());
-
-        $crawler = $this->client->request(Request::METHOD_POST, $link->attr('href').'&ids='.json_encode([$companySegment->getId()], JSON_THROW_ON_ERROR));
+        self::assertSame('Delete selected', $link->text());
+        $url     = $link->attr('href').'?ids='.json_encode([$companySegment->getId()]);
+        $crawler = $this->client->request(Request::METHOD_POST, $url);
         self::assertResponseIsSuccessful();
         self::assertCount(2, $crawler->filter('#companySegmentsTable > tbody > tr'));
     }
 
     public function testClone(): void
     {
-        $this->loadFixtures([LoadCompanySegmentData::class, LoadUserData::class, LoadRoleData::class], false);
+        $this->loadFixtures([LoadCompanySegmentData::class, LoadUserData::class, LoadRoleData::class]);
 
         $segmentName = 'Segment test';
         $crawler     = $this->client->request(Request::METHOD_GET, '/s/company-segments');
diff --git a/Tests/EventListener/ReportSubscriberTest.php b/Tests/EventListener/ReportSubscriberTest.php
index f6ff9b2..aeb1195 100644
--- a/Tests/EventListener/ReportSubscriberTest.php
+++ b/Tests/EventListener/ReportSubscriberTest.php
@@ -196,12 +196,18 @@ public function testOnReportBuilderAddsCompanySegmentsToReportWithCorrectColumns
         $tables = $this->reportBuilderEventMock->getTables();
 
         self::assertArrayHasKey('company_segments', $tables);
+        self::assertIsArray($tables['company_segments']);
+        self::assertArrayHasKey('columns', $tables['company_segments']);
+        self::assertArrayHasKey('filters', $tables['company_segments']);
+        self::assertIsArray($tables['company_segments']['columns']);
+        self::assertIsArray($tables['company_segments']['filters']);
         self::assertCount(16, $tables['company_segments']['columns']);
         self::assertCount(17, $tables['company_segments']['filters']);
 
         $segmentFilter = $tables['company_segments']['filters'][ReportSubscriber::COMPANY_SEGMENTS_XREF_PREFIX.'.segment_id'];
         self::assertIsArray($segmentFilter);
         self::assertArrayHasKey('list', $segmentFilter);
+        self::assertIsArray($segmentFilter['list']);
         self::assertCount(4, $segmentFilter['list']);
         self::assertEquals(['1' => 'a', '2' => 'b', '3' => 'c', '4' => 'd'], $segmentFilter['list']);
     }
@@ -222,18 +228,25 @@ public function testOnReportBuilderHandlesEmptySegmentListCorrectly(): void
 
         $this->reportSubscriber->onReportBuilder($this->reportBuilderEventMock);
         $tables        = $this->reportBuilderEventMock->getTables();
-        $segmentFilter = $tables['company_segments']['filters'][ReportSubscriber::COMPANY_SEGMENTS_XREF_PREFIX.'.segment_id'];
 
         self::assertArrayHasKey('company_segments', $tables);
+        self::assertIsArray($tables['company_segments']);
+        self::assertArrayHasKey('columns', $tables['company_segments']);
+        self::assertIsArray($tables['company_segments']['columns']);
+        self::assertArrayHasKey('filters', $tables['company_segments']);
+        self::assertIsArray($tables['company_segments']['filters']);
         self::assertCount(16, $tables['company_segments']['columns']);
         self::assertCount(17, $tables['company_segments']['filters']);
 
+        $segmentFilter = $tables['company_segments']['filters'][ReportSubscriber::COMPANY_SEGMENTS_XREF_PREFIX.'.segment_id'];
+
         self::assertIsArray($segmentFilter);
         self::assertArrayHasKey('list', $segmentFilter);
         self::assertEmpty($segmentFilter['list']);
 
         self::assertEquals('select', $segmentFilter['type']);
         self::assertArrayHasKey('operators', $segmentFilter);
+        self::assertIsArray($segmentFilter['operators']);
         self::assertCount(4, $segmentFilter['operators']);
     }
 
diff --git a/Tests/Functional/Controller/Api/CompanySegmentApiControllerTest.php b/Tests/Functional/Controller/Api/CompanySegmentApiControllerTest.php
index ba5f43f..6b44eae 100644
--- a/Tests/Functional/Controller/Api/CompanySegmentApiControllerTest.php
+++ b/Tests/Functional/Controller/Api/CompanySegmentApiControllerTest.php
@@ -5,12 +5,14 @@
 use Mautic\CoreBundle\Test\MauticMysqlTestCase;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Entity\CompanySegment;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\EnablePluginTrait;
+use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Tests\HelperCompanySegmentTestTrait;
 use Symfony\Component\HttpFoundation\Request;
 use Symfony\Component\HttpFoundation\Response;
 
 class CompanySegmentApiControllerTest extends MauticMysqlTestCase
 {
     use EnablePluginTrait;
+    use HelperCompanySegmentTestTrait;
 
     protected function setUp(): void
     {
@@ -19,6 +21,7 @@ protected function setUp(): void
         $this->enablePlugin(true);
         $this->useCleanupRollback = false;
         $this->setUpSymfony($this->configParams);
+        $this->loginAdminUser();
     }
 
     public function testGetCompanySegments(): void
@@ -44,6 +47,8 @@ public function testGetCompanySegment(): void
         self::assertSame(Response::HTTP_OK, $response->getStatusCode());
         $data = json_decode($response->getContent(), true);
         self::assertIsArray($data);
+        self::assertIsArray($data['companysegment']);
+        self::assertArrayHasKey('name', $data['companysegment']);
         self::assertSame('Segment test', $data['companysegment']['name']);
     }
 
@@ -81,6 +86,8 @@ public function testEditCompanySegment(): void
         $data = json_decode($this->client->getResponse()->getContent(), true);
         self::assertIsArray($data);
         self::assertIsArray($data['companysegment']);
+        self::assertArrayHasKey('name', $data['companysegment']);
+        self::assertIsString($data['companysegment']['name']);
         self::assertSame('Segment test edited', $data['companysegment']['name']);
         $companySegment = $this->em->getRepository(CompanySegment::class)->find($data['companysegment']['id']);
         self::assertNotNull($companySegment);
@@ -119,6 +126,9 @@ public function testBatchAddCompaniesSegments(): void
         self::assertIsArray($data);
         self::assertIsArray($data['companysegments']);
         self::assertCount(2, $data['companysegments']);
+        self::assertIsArray($data['statusCodes']);
+        self::assertArrayHasKey(0, $data['statusCodes']);
+        self::assertArrayHasKey(1, $data['statusCodes']);
         self::assertSame(Response::HTTP_CREATED, $data['statusCodes'][0]);
         self::assertSame(Response::HTTP_CREATED, $data['statusCodes'][1]);
     }
@@ -143,6 +153,9 @@ public function testAddBatchCompanySegmentOneSuccessAndOneFail(): void
         self::assertIsArray($data);
         self::assertIsArray($data['companysegments']);
         self::assertCount(1, $data['companysegments']);
+        self::assertIsArray($data['statusCodes']);
+        self::assertArrayHasKey(0, $data['statusCodes']);
+        self::assertArrayHasKey(1, $data['statusCodes']);
         self::assertSame(Response::HTTP_CREATED, $data['statusCodes'][0]);
         self::assertSame(Response::HTTP_BAD_REQUEST, $data['statusCodes'][1]);
         self::assertIsArray($data['errors']);
@@ -177,6 +190,10 @@ public function testEditBatchCompanySegmentOneSuccessAndTwoFail(): void
         self::assertIsArray($data);
         self::assertIsArray($data['companysegments']);
         self::assertCount(1, $data['companysegments']);
+        self::assertIsArray($data['statusCodes']);
+        self::assertArrayHasKey(0, $data['statusCodes']);
+        self::assertArrayHasKey(1, $data['statusCodes']);
+        self::assertArrayHasKey(2, $data['statusCodes']);
         self::assertSame(Response::HTTP_OK, $data['statusCodes'][0]);
         self::assertSame(Response::HTTP_NOT_FOUND, $data['statusCodes'][1]);
         self::assertSame(Response::HTTP_NOT_FOUND, $data['statusCodes'][2]);
diff --git a/Tests/Functional/Controller/CompanySegmentControllerTest.php b/Tests/Functional/Controller/CompanySegmentControllerTest.php
index c971fe9..752ebbc 100644
--- a/Tests/Functional/Controller/CompanySegmentControllerTest.php
+++ b/Tests/Functional/Controller/CompanySegmentControllerTest.php
@@ -18,6 +18,7 @@ protected function setUp(): void
         $this->enablePlugin(true);
         $this->useCleanupRollback = false;
         $this->setUpSymfony($this->configParams);
+        $this->loginAdminUser();
     }
 
     public function testNoSuccessDeleteCompanySegmentBecauseInUseInSegment(): void
@@ -45,6 +46,7 @@ public function testNoSuccessDeleteCompanySegmentBecauseInUseInSegment(): void
         self::assertStringContainsString('Company Segment cannot be deleted, it is required by segment', $this->client->getResponse()->getContent());
         $this->client->request('POST', '/s/company-segments/view/'.$companySegmentTBS->getId());
         self::assertIsString($companySegmentTBS->getName());
+        self::assertIsString($this->client->getResponse()->getContent());
         self::assertStringContainsString($companySegmentTBS->getName(), $this->client->getResponse()->getContent());
     }
 
@@ -115,9 +117,10 @@ public function testFailBatchDeleteCompanySegmentBecauseCompanySegmentAreInSegme
         $companySegmentRecord = $this->createCompanySegment('Company Record', 'company-record', true, $filters);
         $url                  = sprintf('/s/company-segments/batchDelete?tmpl=list&ids=["%s","%s"]', $companySegmentGlibi->getId(), $companySegmentTBS->getId());
         $this->client->request('POST', $url);
-        self::assertIsString($this->client->getResponse()->getContent());
-        self::assertStringContainsString('cannot be deleted, it is required by other segments.', $this->client->getResponse()->getContent());
-        self::assertStringContainsString('cannot be deleted, it is required by other company segments.', $this->client->getResponse()->getContent());
-        self::assertStringNotContainsString('has been deleted!', $this->client->getResponse()->getContent());
+        $content = $this->client->getResponse()->getContent();
+        self::assertIsString($content);
+        self::assertStringContainsString('cannot be deleted, it is required by other segments.', $content);
+        self::assertStringContainsString('cannot be deleted, it is required by other company segments.', $content);
+        self::assertStringNotContainsString('has been deleted!', $content);
     }
 }
diff --git a/Tests/Functional/Security/Permissions/CompanySegmentsPermissionsTest.php b/Tests/Functional/Security/Permissions/CompanySegmentsPermissionsTest.php
index 2cfd34e..ecc001a 100644
--- a/Tests/Functional/Security/Permissions/CompanySegmentsPermissionsTest.php
+++ b/Tests/Functional/Security/Permissions/CompanySegmentsPermissionsTest.php
@@ -28,8 +28,12 @@ private function newLogin(User $user, string $password): void
         // Disable the default logging in via username and password.
         $this->clientServer = [];
         $this->setUpSymfony($this->configParams);
-        $this->loginUser($user->getUsername());
-        $this->client->setServerParameter('PHP_AUTH_USER', $user->getUsername());
+        $this->loginUser($user);
+        $username = 'admin';
+        if (is_string($user->getUsername())) {
+            $username = $user->getUsername();
+        }
+        $this->client->setServerParameter('PHP_AUTH_USER', $username);
         $this->client->setServerParameter('PHP_AUTH_PW', $password);
     }
 
diff --git a/Tests/HelperCompanySegmentTestTrait.php b/Tests/HelperCompanySegmentTestTrait.php
index 4d203fc..15f1b17 100644
--- a/Tests/HelperCompanySegmentTestTrait.php
+++ b/Tests/HelperCompanySegmentTestTrait.php
@@ -10,6 +10,7 @@
 use Mautic\LeadBundle\Entity\Company;
 use Mautic\LeadBundle\Entity\Lead;
 use Mautic\LeadBundle\Entity\LeadList;
+use Mautic\UserBundle\Entity\User;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Entity\CompaniesSegments;
 use MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Entity\CompanySegment;
 
@@ -147,4 +148,23 @@ private function createSegment(string $name, string $alias, array $filters = [],
 
         return $segment;
     }
+
+    /**
+     * @return string[]
+     */
+    private function createAjaxHeaders(): array
+    {
+        return [
+            'HTTP_Content-Type'     => 'application/x-www-form-urlencoded; charset=UTF-8',
+            'HTTP_X-Requested-With' => 'XMLHttpRequest',
+            'HTTP_X-CSRF-Token'     => $this->getCsrfToken('mautic_ajax_post'),
+        ];
+    }
+
+    private function loginAdminUser(): void
+    {
+        $user = $this->em->getRepository(User::class)->findOneBy(['username' => 'admin']);
+        assert($user instanceof User);
+        $this->loginUser($user);
+    }
 }
diff --git a/Tests/MauticMysqlTestCase.php b/Tests/MauticMysqlTestCase.php
index c34d343..9581627 100644
--- a/Tests/MauticMysqlTestCase.php
+++ b/Tests/MauticMysqlTestCase.php
@@ -13,6 +13,7 @@
 
 class MauticMysqlTestCase extends \Mautic\CoreBundle\Test\MauticMysqlTestCase
 {
+    use HelperCompanySegmentTestTrait;
     protected ?string $tmpDir = null;
     protected ?string $testHost;
     protected ?ReferenceRepository $fixtures = null;
@@ -94,6 +95,7 @@ protected function loadFixtures(array $classNames = [], bool $append = false): ?
         $executor = parent::loadFixtures($classNames, $append);
         \assert(null !== $executor);
         $this->fixtures = $executor->getReferenceRepository();
+        $this->loginAdminUser();
 
         return $executor;
     }
diff --git a/Tests/NewUserTrait.php b/Tests/NewUserTrait.php
index ed87fc6..eb03233 100644
--- a/Tests/NewUserTrait.php
+++ b/Tests/NewUserTrait.php
@@ -6,7 +6,7 @@
 use Mautic\UserBundle\Entity\Role;
 use Mautic\UserBundle\Entity\User;
 use Mautic\UserBundle\Model\RoleModel;
-use Symfony\Component\Security\Core\Encoder\EncoderFactory;
+use Symfony\Component\PasswordHasher\PasswordHasherInterface;
 
 trait NewUserTrait
 {
@@ -54,10 +54,10 @@ private function createUser(Role $role, string $password='Maut1cR0cks', string $
         $user->setLastName('Doe');
         $user->setUsername($userName);
         $user->setEmail($userName.'@mautic.com');
-        $encoderFactory = self::getContainer()->get('security.encoder_factory');
-        \assert($encoderFactory instanceof EncoderFactory);
-        $encoder = $encoderFactory->getEncoder($user);
-        $user->setPassword($encoder->encodePassword($password, null));
+        $encoderFactory = self::getContainer()->get('security.password_hasher_factory');
+        $hasher         = $encoderFactory->getPasswordHasher($user);
+        \assert($hasher instanceof PasswordHasherInterface);
+        $user->setPassword($hasher->hash($password));
         $user->setRole($role);
         $this->em->persist($user);
         $this->em->flush();
diff --git a/Tests/Segment/Query/Filter/SegmentReferenceFilterQueryBuilderTest.php b/Tests/Segment/Query/Filter/SegmentReferenceFilterQueryBuilderTest.php
index 7563d80..45007fb 100644
--- a/Tests/Segment/Query/Filter/SegmentReferenceFilterQueryBuilderTest.php
+++ b/Tests/Segment/Query/Filter/SegmentReferenceFilterQueryBuilderTest.php
@@ -117,7 +117,7 @@ public function testApplyQuery(string $operator, string $expectedQuery): void
             ->willReturn('comp');
         $queryBuilder = new QueryBuilder($this->connectionMock);
         $queryBuilder->select('1');
-        $queryBuilder->from(MAUTIC_TABLE_PREFIX.'companies', 'comp');
+        $queryBuilder->from($this->getPreTable().'companies', 'comp');
 
         $filter = $this->getContactSegmentFilter($operator, (string) $this->segment->getId());
 
@@ -126,10 +126,15 @@ public function testApplyQuery(string $operator, string $expectedQuery): void
 
         $this->queryBuilder->applyQuery($queryBuilder, $filter);
 
-        $expectedQuery = str_replace('<prefix>', MAUTIC_TABLE_PREFIX, $expectedQuery);
+        $expectedQuery = str_replace('<prefix>', $this->getPreTable(), $expectedQuery);
 
         // Address https://github.com/mautic/mautic/commit/cf7c599e9aa684db7f0c5d9613980608838775b5
-        if (version_compare(constant('MAUTIC_VERSION'), '5.1', 'lt')) {
+        $version = constant('MAUTIC_VERSION');
+        if (!is_string($version)) {
+            $version = '0';
+        }
+
+        if (version_compare($version, '5.1', 'lt')) {
             $expectedQuery = str_replace([
                 'manually_added = 1',
                 'manually_removed = 0',
@@ -148,7 +153,7 @@ public function testApplyQueryWhenSegmentNotExist(): void
             ->willReturn('comp');
         $queryBuilder = new QueryBuilder($this->connectionMock);
         $queryBuilder->select('1');
-        $queryBuilder->from(MAUTIC_TABLE_PREFIX.'companies', 'comp');
+        $queryBuilder->from($this->getPreTable().'companies', 'comp');
 
         $filter = $this->getContactSegmentFilter('eq', 'non_exist_segment_id');
 
@@ -201,4 +206,13 @@ private function getContactSegmentFilter(string $operator, string $parameterValu
             $this->createMock(FilterQueryBuilderInterface::class)
         );
     }
+
+    private function getPreTable(): string
+    {
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            return MAUTIC_TABLE_PREFIX;
+        }
+
+        return '';
+    }
 }
diff --git a/Tests/Service/CompanySegmentServiceTest.php b/Tests/Service/CompanySegmentServiceTest.php
index 2463e1c..d5f4573 100644
--- a/Tests/Service/CompanySegmentServiceTest.php
+++ b/Tests/Service/CompanySegmentServiceTest.php
@@ -86,7 +86,13 @@ public function testRunningCommandWithMatchingCompaniesChangesSegment(string $fi
 
         $companySegmentService = self::getContainer()->get(CompanySegmentService::class);
         \assert($companySegmentService instanceof CompanySegmentService);
-        self::assertSame('1', $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment)[$companySegment->getId()]['count']);
+        $companySegmentsCount = $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment);
+        self::assertIsArray($companySegmentsCount);
+        self::assertIsInt($companySegment->getId());
+        self::assertArrayHasKey($companySegment->getId(), $companySegmentsCount);
+        self::assertArrayHasKey('count', $companySegmentsCount[$companySegment->getId()]);
+        $count = $companySegmentsCount[$companySegment->getId()]['count'];
+        self::assertSame('1', (string) $count);
     }
 
     public static function provideFilterFields(): \Generator
@@ -195,7 +201,13 @@ public function testRunningCommandWithDependentSegmentChangesCompaniesInSegment(
 
         $companySegmentService = self::getContainer()->get(CompanySegmentService::class);
         \assert($companySegmentService instanceof CompanySegmentService);
-        self::assertSame('1', $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment)[$companySegment->getId()]['count']);
+        $companySegmentsCount = $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment);
+        self::assertIsArray($companySegmentsCount);
+        self::assertIsInt($companySegment->getId());
+        self::assertArrayHasKey($companySegment->getId(), $companySegmentsCount);
+        self::assertArrayHasKey('count', $companySegmentsCount[$companySegment->getId()]);
+        $count = $companySegmentsCount[$companySegment->getId()]['count'];
+        self::assertSame('1', (string) $count);
     }
 
     /**
@@ -340,7 +352,13 @@ public function testRunningCommandWithDependentSegmentChangesCompaniesInSegmentW
 
         $companySegmentService = self::getContainer()->get(CompanySegmentService::class);
         \assert($companySegmentService instanceof CompanySegmentService);
-        self::assertSame('0', $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegmentWithFilter)[$companySegmentWithFilter->getId()]['count']);
+        $companySegmentsCount = $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment);
+        self::assertIsArray($companySegmentsCount);
+        self::assertIsInt($companySegment->getId());
+        self::assertArrayHasKey($companySegment->getId(), $companySegmentsCount);
+        self::assertArrayHasKey('count', $companySegmentsCount[$companySegment->getId()]);
+        $count = $companySegmentsCount[$companySegment->getId()]['count'];
+        self::assertSame('0', (string) $count);
     }
 
     /**
@@ -440,7 +458,13 @@ public function testRunningCommandWithDependentWithFiltersSegmentChangesCompanie
 
         $companySegmentService = self::getContainer()->get(CompanySegmentService::class);
         \assert($companySegmentService instanceof CompanySegmentService);
-        self::assertSame('1', $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment)[$companySegment->getId()]['count']);
+        $companySegmentsCount = $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment);
+        self::assertIsArray($companySegmentsCount);
+        self::assertIsInt($companySegment->getId());
+        self::assertArrayHasKey($companySegment->getId(), $companySegmentsCount);
+        self::assertArrayHasKey('count', $companySegmentsCount[$companySegment->getId()]);
+        $count = $companySegmentsCount[$companySegment->getId()]['count'];
+        self::assertSame('1', (string) $count);
 
         // now change company2 to conform to filters and execute query.
         $company1 = $this->getCompany('company-1');
@@ -477,7 +501,13 @@ public function testRunningCommandWithDependentWithFiltersSegmentChangesCompanie
 
         $companySegmentService = self::getContainer()->get(CompanySegmentService::class);
         \assert($companySegmentService instanceof CompanySegmentService);
-        self::assertSame('1', $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment)[$companySegment->getId()]['count']);
+        $companySegmentsCount = $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment);
+        self::assertIsArray($companySegmentsCount);
+        self::assertIsInt($companySegment->getId());
+        self::assertArrayHasKey($companySegment->getId(), $companySegmentsCount);
+        self::assertArrayHasKey('count', $companySegmentsCount[$companySegment->getId()]);
+        $count = $companySegmentsCount[$companySegment->getId()]['count'];
+        self::assertSame('1', (string) $count);
 
         // remove conforming filter from company2
         $company2 = $this->getCompany('company-2');
@@ -509,7 +539,13 @@ public function testRunningCommandWithDependentWithFiltersSegmentChangesCompanie
 
         $companySegmentService = self::getContainer()->get(CompanySegmentService::class);
         \assert($companySegmentService instanceof CompanySegmentService);
-        self::assertSame('0', $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment)[$companySegment->getId()]['count']);
+        $companySegmentsCount = $companySegmentService->getTotalCompanySegmentsCompaniesCount($companySegment);
+        self::assertIsArray($companySegmentsCount);
+        self::assertIsInt($companySegment->getId());
+        self::assertArrayHasKey($companySegment->getId(), $companySegmentsCount);
+        self::assertArrayHasKey('count', $companySegmentsCount[$companySegment->getId()]);
+        $count = $companySegmentsCount[$companySegment->getId()]['count'];
+        self::assertSame('0', (string) $count);
     }
 
     /**
diff --git a/composer.json b/composer.json
index 817e0b8..7c8cb6c 100644
--- a/composer.json
+++ b/composer.json
@@ -15,24 +15,27 @@
     "keywords": ["mautic","plugin","integration","segments", "company"],
 
     "require": {
-        "php": "^8.0",
+        "php": "^8.1.0",
         "ext-json": "*",
-        "mautic/core-lib": "^5.0"
+        "mautic/core-lib": "^6.0"
     },
     "extra": {
         "install-directory-name": "LeuchtfeuerCompanySegmentsBundle"
     },
     "require-dev": {
-        "liip/functional-test-bundle": "^4.2",
-        "liip/test-fixtures-bundle": "^1.6",
+        "liip/functional-test-bundle": "^4.3",
+        "liip/test-fixtures-bundle": "^2.6",
         "phpunit/phpunit": "^9.5",
-        "friendsofphp/php-cs-fixer": "^3.4",
-        "symfony/browser-kit": "^5.4",
-        "phpstan/phpstan": "^1.0",
-        "phpstan/phpstan-deprecation-rules": "^1.0",
-        "phpstan/phpstan-phpunit": "^1.0",
-        "phpstan/phpstan-strict-rules": "^1.0",
-        "rector/rector": "^1.0.4"
+        "friendsofphp/php-cs-fixer": "^3.14",
+        "symfony/browser-kit": "^6.4.0",
+        "phpstan/phpstan": "^2.0",
+        "phpstan/phpstan-deprecation-rules": "^2.0",
+        "phpstan/phpstan-doctrine": "^2.0",
+        "phpstan/phpstan-phpunit": "^2.0",
+        "phpstan/phpstan-symfony": "^2.0",
+        "phpstan/phpstan-strict-rules": "^2.0",
+
+        "rector/rector": "2.0"
     },
     "autoload": {
         "psr-4": {
@@ -60,7 +63,7 @@
     ],
     "scripts": {
         "fixcs": "./vendor/bin/php-cs-fixer fix --config .php-cs-fixer.php --show-progress dots -v",
-        "phpstan": "./vendor/bin/phpstan"
+        "phpstan": "./vendor/bin/phpstan analyse --ansi"
     },
     "config": {
         "allow-plugins": {
diff --git a/phpstan.neon b/phpstan.neon
index 2644de8..0fb1103 100644
--- a/phpstan.neon
+++ b/phpstan.neon
@@ -62,3 +62,5 @@ parameters:
           count: 1
           path: Model/CompanySegmentModel.php
           reportUnmatched: false
+        - message: "#Call to method .* of deprecated class Mautic\\\\CampaignBundle\\\\Event\\\\CampaignExecutionEvent#"
+          reportUnmatched: false
\ No newline at end of file

From df0a8cf78b2ca04cb3b33113fed80650873ed60e Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Mon, 27 Oct 2025 23:43:43 +0000
Subject: [PATCH 03/12] fix rector

---
 Controller/Api/CompanySegmentApiController.php | 2 +-
 Model/CompanySegmentModel.php                  | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/Controller/Api/CompanySegmentApiController.php b/Controller/Api/CompanySegmentApiController.php
index 9bf785a..1896d50 100644
--- a/Controller/Api/CompanySegmentApiController.php
+++ b/Controller/Api/CompanySegmentApiController.php
@@ -132,7 +132,7 @@ public function deleteEntitiesAction(Request $request)
 
             try {
                 $json = json_encode($result, JSON_THROW_ON_ERROR);
-            } catch (\JsonException $e) {
+            } catch (\JsonException) {
                 $json = json_encode(
                     ['errors' => $this->translator->trans('mautic.core.error.json_encode_failed')]
                 );
diff --git a/Model/CompanySegmentModel.php b/Model/CompanySegmentModel.php
index c3fdee7..d3f4839 100644
--- a/Model/CompanySegmentModel.php
+++ b/Model/CompanySegmentModel.php
@@ -586,7 +586,7 @@ public function canNotBeDeleted(array $segmentIds, bool $isContactSegment = fals
                     continue;
                 }
 
-                if (!array_key_exists('properties', $eachFilter) || !array_key_exists('filter', $eachFilter['properties'])) {
+                if (!array_key_exists('filter', $eachFilter['properties'])) {
                     continue;
                 }
                 /** @var array<int> $filterValue */

From e7a30acb13f4d30dee68041e45aff7feb8fda340 Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Mon, 27 Oct 2025 23:56:57 +0000
Subject: [PATCH 04/12] fix rector

---
 .github/workflows/ci.yaml | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/.github/workflows/ci.yaml b/.github/workflows/ci.yaml
index f4fd6d8..fa10126 100644
--- a/.github/workflows/ci.yaml
+++ b/.github/workflows/ci.yaml
@@ -157,7 +157,7 @@ jobs:
       matrix:
         commands: ['PHPStan', 'CS Fixer', 'Rector', 'Twig Lint']
         php-versions: ['8.1']
-        mautic-versions: ['5.x']
+        mautic-versions: ['6.x']
 
     name: ${{ matrix.commands }} - ${{ matrix.php-versions }} - ${{ matrix.mautic-versions }}
 

From fbebbfd59f6146b9a56d4d25d3c1e67cec090fc2 Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Tue, 28 Oct 2025 00:05:20 +0000
Subject: [PATCH 05/12] fix ci

---
 .github/workflows/ci.yaml | 18 +++++++++++-------
 1 file changed, 11 insertions(+), 7 deletions(-)

diff --git a/.github/workflows/ci.yaml b/.github/workflows/ci.yaml
index fa10126..b35e8f0 100644
--- a/.github/workflows/ci.yaml
+++ b/.github/workflows/ci.yaml
@@ -16,17 +16,21 @@ jobs:
     runs-on: ubuntu-latest
     strategy:
       matrix:
-        php-versions: ['8.0', '8.1']
+        php-versions: ['8.1', '8.2', '8.3']
         db-types: ['mysql', 'mariadb']
-        mautic-versions: ['5.0', '5.1', '5.x']
+        mautic-versions: ['6.0', '6.x']
         include:
-          - mautic-versions: 5.1
+          - mautic-versions: 6.0
+            php-versions: 8.1
+          - mautic-versions: 6.x
+            php-versions: 8.1
+          - mautic-versions: 6.0
             php-versions: 8.2
-          - mautic-versions: 5.x
+          - mautic-versions: 6.x
             php-versions: 8.2
-          - mautic-versions: 5.x
+          - mautic-versions: 6.0
             php-versions: 8.3
-          - mautic-versions: 5.2
+          - mautic-versions: 6.x
             php-versions: 8.3
         exclude:
           - mautic-versions: 5.x # 5.2 will not support php-8.0
@@ -247,7 +251,7 @@ jobs:
       matrix:
         commands: [ 'PHPStan', 'CS Fixer', 'Rector' ]
         php-versions: [ '8.3' ]
-        mautic-versions: [ '5.x' ]
+        mautic-versions: [ '6.x', '6.0' ]
 
     name: ${{ matrix.commands }} - ${{ matrix.php-versions }} - ${{ matrix.mautic-versions }}
 

From 3f94429fb101e491804ed3107ccbe4b28ea61760 Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Tue, 28 Oct 2025 00:09:57 +0000
Subject: [PATCH 06/12] fix ci

---
 .github/workflows/ci.yaml | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/.github/workflows/ci.yaml b/.github/workflows/ci.yaml
index b35e8f0..1ef628f 100644
--- a/.github/workflows/ci.yaml
+++ b/.github/workflows/ci.yaml
@@ -160,8 +160,8 @@ jobs:
       fail-fast: false
       matrix:
         commands: ['PHPStan', 'CS Fixer', 'Rector', 'Twig Lint']
-        php-versions: ['8.1']
-        mautic-versions: ['6.x']
+        php-versions: ['8.1','8.2','8.3']
+        mautic-versions: ['6.x','6.0']
 
     name: ${{ matrix.commands }} - ${{ matrix.php-versions }} - ${{ matrix.mautic-versions }}
 

From 2f2caeaf2166a4f0b05d529f8ec6586bc86f0068 Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Tue, 28 Oct 2025 00:21:11 +0000
Subject: [PATCH 07/12] fix ci

---
 .github/workflows/ci.yaml | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/.github/workflows/ci.yaml b/.github/workflows/ci.yaml
index 1ef628f..a772f3f 100644
--- a/.github/workflows/ci.yaml
+++ b/.github/workflows/ci.yaml
@@ -160,8 +160,8 @@ jobs:
       fail-fast: false
       matrix:
         commands: ['PHPStan', 'CS Fixer', 'Rector', 'Twig Lint']
-        php-versions: ['8.1','8.2','8.3']
-        mautic-versions: ['6.x','6.0']
+        php-versions: ['8.3']
+        mautic-versions: ['6.0']
 
     name: ${{ matrix.commands }} - ${{ matrix.php-versions }} - ${{ matrix.mautic-versions }}
 

From 1f5ccdc4c1f307a8923494e3d1383a7ea65e695e Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Tue, 28 Oct 2025 00:27:13 +0000
Subject: [PATCH 08/12] fix ci

---
 .github/workflows/ci.yaml | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/.github/workflows/ci.yaml b/.github/workflows/ci.yaml
index a772f3f..17a396c 100644
--- a/.github/workflows/ci.yaml
+++ b/.github/workflows/ci.yaml
@@ -251,7 +251,7 @@ jobs:
       matrix:
         commands: [ 'PHPStan', 'CS Fixer', 'Rector' ]
         php-versions: [ '8.3' ]
-        mautic-versions: [ '6.x', '6.0' ]
+        mautic-versions: [ '6.x']
 
     name: ${{ matrix.commands }} - ${{ matrix.php-versions }} - ${{ matrix.mautic-versions }}
 

From 14ba47dd864d9c3895343ab197474974bb805f02 Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Tue, 28 Oct 2025 20:21:02 +0000
Subject: [PATCH 09/12] - phpstan - fixcs

---
 Controller/BatchSegmentController.php         |  9 ++++++--
 Entity/CompanyEventLogRepository.php          | 12 ++++++----
 Event/CompanySegmentFilteringEvent.php        | 13 +++++++++--
 Event/CompanySegmentPostDelete.php            |  2 +-
 Event/CompanySegmentPostSave.php              |  2 +-
 EventListener/CampaignSubscriber.php          | 23 +++++++++++++++----
 EventListener/CompanySearchSubscriber.php     | 13 +++++++++--
 Form/Type/CompanySegmentActionType.php        |  5 ----
 Form/Type/CompanySegmentListType.php          |  5 ----
 Form/Type/FilterType.php                      |  9 +++++++-
 Model/CompanyFieldModelDecorated.php          | 20 +++++++++++++++-
 Model/CompanySegmentActionModel.php           |  8 +++----
 Segment/Query/CompanySegmentQueryBuilder.php  | 21 +++++++++++++++--
 Service/CompanySegmentService.php             |  8 +++++--
 Tests/Controller/AjaxControllerTest.php       |  1 +
 .../CompanySegmentControllerTest.php          | 12 +++++++++-
 .../UpdateCompanySegmentsCommandTest.php      |  2 --
 Tests/NewUserTrait.php                        |  9 +++++---
 Tests/Service/CompanySegmentServiceTest.php   |  1 +
 .../Constraints/UniqueAliasValidatorTest.php  |  3 +++
 .../Constraints/SegmentDateValidator.php      |  4 ++--
 21 files changed, 137 insertions(+), 45 deletions(-)

diff --git a/Controller/BatchSegmentController.php b/Controller/BatchSegmentController.php
index 1b1e385..1f4a1e7 100644
--- a/Controller/BatchSegmentController.php
+++ b/Controller/BatchSegmentController.php
@@ -52,11 +52,16 @@ public function setAction(Request $request): JsonResponse
         } else {
             $params = $requestParameters['company_batch'];
         }
-
-        $companyIds = '' === $params['ids'] ? [] : json_decode($params['ids'], true, 512, JSON_THROW_ON_ERROR);
+        $companyIds = [];
+        if (is_string($params['ids']) && '' !== $params['ids']) {
+            /** @var array<int> $companyIds */
+            $companyIds = json_decode($params['ids'], true, 512, JSON_THROW_ON_ERROR);
+        }
 
         if ([] !== $companyIds && is_array($companyIds)) {
+            /** @var array<int> $segmentsToAdd */
             $segmentsToAdd    = $params['add'] ?? [];
+            /** @var array<int> $segmentsToRemove */
             $segmentsToRemove = $params['remove'] ?? [];
 
             if (is_array($segmentsToAdd) && [] !== $segmentsToAdd) {
diff --git a/Entity/CompanyEventLogRepository.php b/Entity/CompanyEventLogRepository.php
index d8ea7a6..361aae7 100644
--- a/Entity/CompanyEventLogRepository.php
+++ b/Entity/CompanyEventLogRepository.php
@@ -21,10 +21,14 @@ class CompanyEventLogRepository extends CommonRepository
      */
     public function updateCompany($fromCompanyId, $toCompanyId): void
     {
-        $toCompanyId = (int) $toCompanyId;
-        $toCompanyId = (string) $toCompanyId;
-        $q           = $this->_em->getConnection()->createQueryBuilder();
-        $q->update(MAUTIC_TABLE_PREFIX.'company_event_log')
+        $toCompanyId  = (int) $toCompanyId;
+        $toCompanyId  = (string) $toCompanyId;
+        $q            = $this->_em->getConnection()->createQueryBuilder();
+        $preTableName = '';
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            $preTableName = MAUTIC_TABLE_PREFIX;
+        }
+        $q->update($preTableName.'company_event_log')
             ->set('company_id', $toCompanyId)
             ->where('company_id = '.(int) $fromCompanyId)
             ->executeStatement();
diff --git a/Event/CompanySegmentFilteringEvent.php b/Event/CompanySegmentFilteringEvent.php
index 7e5bd17..6ddba7a 100644
--- a/Event/CompanySegmentFilteringEvent.php
+++ b/Event/CompanySegmentFilteringEvent.php
@@ -29,7 +29,11 @@ public function __construct(
         $this->em              = $entityManager;
         $this->isFilteringDone = false;
         $this->subQuery        = '';
-        $tableAlias            = $queryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+        $preTableName          = '';
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            $preTableName = MAUTIC_TABLE_PREFIX;
+        }
+        $tableAlias            = $queryBuilder->getTableAlias($preTableName.'companies');
         \assert(is_string($tableAlias));
         $this->companiesTableAlias = $tableAlias;
     }
@@ -49,7 +53,12 @@ public function getAlias(): string
 
     public function getFunc(): string
     {
-        return $this->details->getArray()['operator'];
+        $detailsList = $this->details->getArray();
+        if (!array_key_exists('operator', $detailsList) || !is_string($detailsList['operator'])) {
+            return '';
+        }
+
+        return $detailsList['operator'];
     }
 
     public function getEntityManager(): EntityManagerInterface
diff --git a/Event/CompanySegmentPostDelete.php b/Event/CompanySegmentPostDelete.php
index 2ac8eb8..4558b08 100644
--- a/Event/CompanySegmentPostDelete.php
+++ b/Event/CompanySegmentPostDelete.php
@@ -10,7 +10,7 @@
 class CompanySegmentPostDelete extends CompanySegmentEvent
 {
     /**
-     * @return array<string, mixed>
+     * @return array<mixed>
      */
     public function getChanges(): array
     {
diff --git a/Event/CompanySegmentPostSave.php b/Event/CompanySegmentPostSave.php
index e0527e4..837dd90 100644
--- a/Event/CompanySegmentPostSave.php
+++ b/Event/CompanySegmentPostSave.php
@@ -10,7 +10,7 @@
 class CompanySegmentPostSave extends CompanySegmentPreSavedEvent
 {
     /**
-     * @return array<string, mixed>
+     * @return array<mixed>
      */
     public function getChanges(): array
     {
diff --git a/EventListener/CampaignSubscriber.php b/EventListener/CampaignSubscriber.php
index 81099f1..a1793bb 100644
--- a/EventListener/CampaignSubscriber.php
+++ b/EventListener/CampaignSubscriber.php
@@ -60,8 +60,9 @@ public function onCampaignActionTriggerAction(CampaignExecutionEvent $event): Ca
         if (!$this->config->isPublished() || !$event->checkContext(self::MANAGE_COMPANY_SEGMENT_ACTION)) {
             return $event->setResult(false);
         }
-
+        /** @var array<int> */
         $addTo      = $event->getConfig()['addToLists'];
+        /** @var array<int> */
         $removeFrom = $event->getConfig()['removeFromLists'];
 
         $lead              = $event->getLead();
@@ -72,12 +73,24 @@ public function onCampaignActionTriggerAction(CampaignExecutionEvent $event): Ca
             return $event->setResult(true);
         }
 
-        if ([] !== $addTo && is_array($primaryCompany) && array_key_exists('id', $primaryCompany) && '' !== $primaryCompany['id'] && null !== $primaryCompany['id']) {
-            $somethingHappened = $this->addRemoveCompanyToSegment($addTo, (int) $primaryCompany['id'], true);
+        $rawId = null;
+        if (is_array($primaryCompany) && array_key_exists('id', $primaryCompany)) {
+            $rawId = $primaryCompany['id'];
+        }
+
+        $primaryCompanyId = null;
+        if (is_int($rawId)) {
+            $primaryCompanyId = $rawId;
+        } elseif (is_string($rawId) && ctype_digit($rawId)) {
+            $primaryCompanyId = (int) $rawId;
+        }
+
+        if ([] !== $addTo && null !== $primaryCompanyId) {
+            $somethingHappened = $this->addRemoveCompanyToSegment($addTo, $primaryCompanyId, true);
         }
 
-        if ([] !== $removeFrom && is_array($primaryCompany) && array_key_exists('id', $primaryCompany) && '' !== $primaryCompany['id'] && null !== $primaryCompany['id']) {
-            $somethingHappened = $this->addRemoveCompanyToSegment($removeFrom, (int) $primaryCompany['id'], false);
+        if ([] !== $removeFrom && null !== $primaryCompanyId) {
+            $somethingHappened = $this->addRemoveCompanyToSegment($removeFrom, $primaryCompanyId, false);
         }
 
         return $event->setResult($somethingHappened);
diff --git a/EventListener/CompanySearchSubscriber.php b/EventListener/CompanySearchSubscriber.php
index 5c82b11..3203be2 100644
--- a/EventListener/CompanySearchSubscriber.php
+++ b/EventListener/CompanySearchSubscriber.php
@@ -46,7 +46,7 @@ public function onBuildSearch(CompanyBuildSearchEvent $event): void
 
         $sq = (new QueryBuilder($this->connection));
         $sq->select('1')
-            ->from(MAUTIC_TABLE_PREFIX.CompaniesSegments::TABLE_NAME, CompaniesSegments::RELATIONS_NAME)
+            ->from($this->getPreTable().CompaniesSegments::TABLE_NAME, CompaniesSegments::RELATIONS_NAME)
             ->where(
                 $sq->expr()->and(
                     $sq->expr()->eq(
@@ -101,7 +101,7 @@ private function getCompanySegmentIdsByAlias(string $segmentAlias): array
     {
         $result = (new QueryBuilder($this->connection))
             ->select(CompanySegment::DEFAULT_ALIAS.'.id')
-            ->from(MAUTIC_TABLE_PREFIX.CompanySegment::TABLE_NAME, CompanySegment::DEFAULT_ALIAS)
+            ->from($this->getPreTable().CompanySegment::TABLE_NAME, CompanySegment::DEFAULT_ALIAS)
             ->where(CompanySegment::DEFAULT_ALIAS.'.alias = :alias')
             ->setParameter('alias', $segmentAlias)
             ->executeQuery()
@@ -118,4 +118,13 @@ private function getCompanySegmentIdsByAlias(string $segmentAlias): array
 
         return $return;
     }
+
+    private function getPreTable(): string
+    {
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            return MAUTIC_TABLE_PREFIX;
+        }
+
+        return '';
+    }
 }
diff --git a/Form/Type/CompanySegmentActionType.php b/Form/Type/CompanySegmentActionType.php
index 6efb553..ed639a1 100644
--- a/Form/Type/CompanySegmentActionType.php
+++ b/Form/Type/CompanySegmentActionType.php
@@ -5,11 +5,6 @@
 use Symfony\Component\Form\AbstractType;
 use Symfony\Component\Form\FormBuilderInterface;
 
-/**
- * @phpstan-ignore-next-line
- *
- * @extends AbstractType<mixed>
- */
 class CompanySegmentActionType extends AbstractType
 {
     public function buildForm(FormBuilderInterface $builder, array $options): void
diff --git a/Form/Type/CompanySegmentListType.php b/Form/Type/CompanySegmentListType.php
index e9430c3..b4ecb2f 100644
--- a/Form/Type/CompanySegmentListType.php
+++ b/Form/Type/CompanySegmentListType.php
@@ -9,11 +9,6 @@
 use Symfony\Component\OptionsResolver\Options;
 use Symfony\Component\OptionsResolver\OptionsResolver;
 
-/**
- * @phpstan-ignore-next-line
- *
- * @extends AbstractType<mixed>
- */
 class CompanySegmentListType extends AbstractType
 {
     public function __construct(
diff --git a/Form/Type/FilterType.php b/Form/Type/FilterType.php
index d0e7671..a9286da 100644
--- a/Form/Type/FilterType.php
+++ b/Form/Type/FilterType.php
@@ -95,7 +95,14 @@ public function buildForm(FormBuilderInterface $builder, array $options): void
             $filterPropertiesType = $form->get('properties');
             $filterPropertiesType->setData($data['properties'] ?? []);
 
-            if (null !== $fieldAlias && '' !== $fieldAlias && null !== $operator && '' !== $operator) {
+            if (
+                null !== $fieldAlias
+                && '' !== $fieldAlias
+                && null !== $operator
+                && '' !== $operator
+                && is_string($fieldObject)
+                && is_string($operator)
+            ) {
                 $this->formAdjustmentsProvider->adjustForm(
                     $filterPropertiesType,
                     $fieldAlias,
diff --git a/Model/CompanyFieldModelDecorated.php b/Model/CompanyFieldModelDecorated.php
index 2d19975..3c70f95 100644
--- a/Model/CompanyFieldModelDecorated.php
+++ b/Model/CompanyFieldModelDecorated.php
@@ -37,6 +37,7 @@ public function getPublishedFieldArrays($object = 'lead'): array
             ]
         );
 
+        /** @var array<array<string, mixed>> $rows */
         $rows = [];
         if ($entities instanceof Paginator) {
             $rows = iterator_to_array($entities->getIterator(), true); // preserve keys
@@ -48,11 +49,28 @@ public function getPublishedFieldArrays($object = 'lead'): array
             if (
                 is_array($row)
                 && array_key_exists('properties', $row)
+                && is_array($row['properties'])
                 && array_key_exists('list', $row['properties'])
                 && is_array($row['properties']['list'])
                 && [] !== $row['properties']['list']
             ) {
-                uasort($row['properties']['list'], fn ($a, $b): int => strcasecmp($a['value'] ?? '', $b['value'] ?? ''));
+                uasort(
+                    $row['properties']['list'],
+                    function ($a, $b): int {
+                        $va = '';
+                        $vb = '';
+
+                        if (is_array($a) && array_key_exists('value', $a) && is_string($a['value'])) {
+                            $va = $a['value'];
+                        }
+
+                        if (is_array($b) && array_key_exists('value', $b) && is_string($b['value'])) {
+                            $vb = $b['value'];
+                        }
+
+                        return strcasecmp($va, $vb);
+                    }
+                );
                 $row['properties']['list'] = array_values($row['properties']['list']);
                 $rows[$k]                  = $row;
             }
diff --git a/Model/CompanySegmentActionModel.php b/Model/CompanySegmentActionModel.php
index 734589f..e9fd5fe 100644
--- a/Model/CompanySegmentActionModel.php
+++ b/Model/CompanySegmentActionModel.php
@@ -18,8 +18,8 @@ public function __construct(
     }
 
     /**
-     * @param array<mixed,mixed> $companyIds
-     * @param array<mixed,mixed> $segmentIds
+     * @param array<int> $companyIds
+     * @param array<int> $segmentIds
      */
     public function addCompanies(array $companyIds, array $segmentIds, bool $manuallyAdded = true): void
     {
@@ -35,8 +35,8 @@ public function addCompanies(array $companyIds, array $segmentIds, bool $manuall
     }
 
     /**
-     * @param array<mixed,mixed> $companyIds
-     * @param array<mixed,mixed> $segmentIds
+     * @param array<int> $companyIds
+     * @param array<int> $segmentIds
      */
     public function removeCompanies(array $companyIds, array $segmentIds, bool $manuallyRemoved = true): void
     {
diff --git a/Segment/Query/CompanySegmentQueryBuilder.php b/Segment/Query/CompanySegmentQueryBuilder.php
index 73ed37f..b9e5442 100644
--- a/Segment/Query/CompanySegmentQueryBuilder.php
+++ b/Segment/Query/CompanySegmentQueryBuilder.php
@@ -420,8 +420,25 @@ private function getSegmentEdges(CompanySegment $companySegment): array
 
         foreach ($segmentFilters as $segmentFilter) {
             if (isset($segmentFilter['field']) && CompanySegmentModel::PROPERTIES_FIELD === $segmentFilter['field']) {
-                $bcFilter     = $segmentFilter['filter'] ?? [];
-                $filterEdges  = $segmentFilter['properties']['filter'] ?? $bcFilter;
+                $filterEdges = [];
+                if (
+                    is_array($segmentFilter)
+                    && array_key_exists('filter', $segmentFilter)
+                    && is_array($segmentFilter['filter'])
+                ) {
+                    $filterEdges     = $segmentFilter['filter'];
+                }
+
+                if (
+                    is_array($segmentFilter)
+                    && array_key_exists('properties', $segmentFilter)
+                    && is_array($segmentFilter['properties'])
+                    && array_key_exists('filter', $segmentFilter['properties'])
+                    && is_array($segmentFilter['properties']['filter'])
+                ) {
+                    $filterEdges  = $segmentFilter['properties']['filter'];
+                }
+
                 $segmentEdges = array_merge($segmentEdges, $filterEdges);
             }
         }
diff --git a/Service/CompanySegmentService.php b/Service/CompanySegmentService.php
index 2db48d3..777f22f 100644
--- a/Service/CompanySegmentService.php
+++ b/Service/CompanySegmentService.php
@@ -189,8 +189,12 @@ public function getOrphanedCompanySegmentCompanies(CompanySegment $segment, arra
      */
     private function getNewCompanySegmentCompaniesQueryBuilder(CompanySegment $segment, array $batchLimiters, bool $addNewContactsRestrictions = true): QueryBuilder
     {
-        $queryBuilder        = $this->getNewSegmentContactsQuery($segment, $batchLimiters, $addNewContactsRestrictions);
-        $companiesTableAlias = $queryBuilder->getTableAlias(MAUTIC_TABLE_PREFIX.'companies');
+        $queryBuilder         = $this->getNewSegmentContactsQuery($segment, $batchLimiters, $addNewContactsRestrictions);
+        $preTableName         = '';
+        if (is_string(MAUTIC_TABLE_PREFIX)) {
+            $preTableName = MAUTIC_TABLE_PREFIX;
+        }
+        $companiesTableAlias = $queryBuilder->getTableAlias($preTableName.'companies');
         \assert(is_string($companiesTableAlias));
 
         // Prepend the DISTINCT to the beginning of the select array
diff --git a/Tests/Controller/AjaxControllerTest.php b/Tests/Controller/AjaxControllerTest.php
index f845cb9..38522e4 100644
--- a/Tests/Controller/AjaxControllerTest.php
+++ b/Tests/Controller/AjaxControllerTest.php
@@ -43,6 +43,7 @@ public function testCompanySegmentFilter(): void
 
         self::assertIsArray($json);
         self::assertArrayHasKey('viewParameters', $json);
+        self::assertIsArray($json['viewParameters']);
         self::assertArrayHasKey('form', $json['viewParameters']);
         self::assertIsString($json['viewParameters']['form']);
         self::assertStringContainsString('company_segments_filters_1_properties_filter', $json['viewParameters']['form']);
diff --git a/Tests/Controller/CompanySegmentControllerTest.php b/Tests/Controller/CompanySegmentControllerTest.php
index 59a4e43..811a992 100644
--- a/Tests/Controller/CompanySegmentControllerTest.php
+++ b/Tests/Controller/CompanySegmentControllerTest.php
@@ -76,8 +76,18 @@ public function testCreateCancel(): void
             'company_segments' => [],
         ]);
         $values = $form->getPhpValues();
+        self::assertIsArray($values);
+        self::assertArrayHasKey('company_segments', $values);
+        self::assertIsArray($values['company_segments']);
+        if (!array_key_exists('buttons', $values['company_segments'])) {
+            $values['company_segments']['buttons'] = [];
+        }
+        self::assertArrayHasKey('buttons', $values['company_segments']);
+        self::assertIsArray($values['company_segments']['buttons']);
+        if (!array_key_exists('cancel', $values['company_segments']['buttons'])) {
+            $values['company_segments']['buttons']['cancel'] = '';
+        }
 
-        $values['company_segments']['buttons']['cancel'] = '';
         $this->client->request($form->getMethod(), $form->getUri(), $values);
         self::assertResponseIsSuccessful();
 
diff --git a/Tests/Functional/Commands/UpdateCompanySegmentsCommandTest.php b/Tests/Functional/Commands/UpdateCompanySegmentsCommandTest.php
index 5e4b557..645904a 100644
--- a/Tests/Functional/Commands/UpdateCompanySegmentsCommandTest.php
+++ b/Tests/Functional/Commands/UpdateCompanySegmentsCommandTest.php
@@ -66,8 +66,6 @@ public function testUpdateCompanySegmentsCommandAddItemInNewSegment(): void
 
         $resultCompaniesSegmentsAfter = $this->em->getRepository(CompaniesSegments::class)->findAll();
         self::assertCount(2, $resultCompaniesSegmentsAfter);
-        assert($resultCompaniesSegmentsAfter[0] instanceof CompaniesSegments);
-        assert($resultCompaniesSegmentsAfter[1] instanceof CompaniesSegments);
         self::assertEquals($resultCompaniesSegmentsAfter[0]->getCompany()->getId(), $resultCompaniesSegmentsAfter[1]->getCompany()->getId());
         self::assertEquals($resultCompaniesSegmentsAfter[1]->getCompanySegment()->getId(), $companySegmentTwo->getId());
     }
diff --git a/Tests/NewUserTrait.php b/Tests/NewUserTrait.php
index eb03233..bf08059 100644
--- a/Tests/NewUserTrait.php
+++ b/Tests/NewUserTrait.php
@@ -55,9 +55,12 @@ private function createUser(Role $role, string $password='Maut1cR0cks', string $
         $user->setUsername($userName);
         $user->setEmail($userName.'@mautic.com');
         $encoderFactory = self::getContainer()->get('security.password_hasher_factory');
-        $hasher         = $encoderFactory->getPasswordHasher($user);
-        \assert($hasher instanceof PasswordHasherInterface);
-        $user->setPassword($hasher->hash($password));
+        if (method_exists($encoderFactory, 'getPasswordHasher')) {
+            $hasher         = $encoderFactory->getPasswordHasher($user);
+            \assert($hasher instanceof PasswordHasherInterface);
+            $user->setPassword($hasher->hash($password));
+        }
+
         $user->setRole($role);
         $this->em->persist($user);
         $this->em->flush();
diff --git a/Tests/Service/CompanySegmentServiceTest.php b/Tests/Service/CompanySegmentServiceTest.php
index d5f4573..fe7d709 100644
--- a/Tests/Service/CompanySegmentServiceTest.php
+++ b/Tests/Service/CompanySegmentServiceTest.php
@@ -237,6 +237,7 @@ public function testRunningCommandWithDependentSegmentChangesCompaniesInSegmentW
         self::assertCount(1, $existingFilters, 'Self-check.');
         self::assertIsArray($existingFilters[0], 'Self-check.');
         self::assertSame(CompanySegmentModel::PROPERTIES_FIELD, $existingFilters[0]['field'], 'Self-check.');
+        self::assertIsArray($existingFilters[0]['properties']['filter']);
         $existingFilters[0]['properties']['filter'][0] = ''.$companySegmentWithFilter->getId();
         $companySegment->setFilters($existingFilters);
         $companySegmentModel->saveEntity($companySegment);
diff --git a/Tests/Validator/Constraints/UniqueAliasValidatorTest.php b/Tests/Validator/Constraints/UniqueAliasValidatorTest.php
index c3d1f3d..57e928c 100644
--- a/Tests/Validator/Constraints/UniqueAliasValidatorTest.php
+++ b/Tests/Validator/Constraints/UniqueAliasValidatorTest.php
@@ -17,6 +17,9 @@
 use Symfony\Component\Validator\Exception\UnexpectedValueException;
 use Symfony\Component\Validator\Test\ConstraintValidatorTestCase;
 
+/**
+ * @extends \Symfony\Component\Validator\Test\ConstraintValidatorTestCase<\MauticPlugin\LeuchtfeuerCompanySegmentsBundle\Validator\Constraints\UniqueAliasValidator>
+ */
 class UniqueAliasValidatorTest extends ConstraintValidatorTestCase
 {
     /**
diff --git a/Validator/Constraints/SegmentDateValidator.php b/Validator/Constraints/SegmentDateValidator.php
index 579d28f..547e882 100644
--- a/Validator/Constraints/SegmentDateValidator.php
+++ b/Validator/Constraints/SegmentDateValidator.php
@@ -18,7 +18,7 @@ public function __construct(
     }
 
     /**
-     * @phpstan-param array<mixed>|mixed $filters
+     * @phpstan-param array<int, array<string, mixed>>|mixed $filters
      */
     public function validate($filters, Constraint $constraint): void
     {
@@ -30,7 +30,7 @@ public function validate($filters, Constraint $constraint): void
             if (!is_array($filter)) {
                 continue;
             }
-
+            /** @var array<string, mixed> $filter */
             if (isset($filter['type']) && in_array($filter['type'], ['date', 'datetime'], true)) {
                 $segmentFilter  = $this->contactSegmentFilterFactory->factorSegmentFilter($filter);
                 $parameterValue = $segmentFilter->getParameterValue();

From 14d286d1c6ec3b30db9f9d471aa38f46bb746006 Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Mon, 3 Nov 2025 22:49:48 +0000
Subject: [PATCH 10/12] - phpstan - fixcs

---
 .../Api/CompanySegmentApiController.php       |  2 ++
 Controller/CompanySegmentController.php       | 16 +++++++--
 Entity/CompanySegment.php                     |  1 +
 Entity/CompanySegmentRepository.php           |  2 ++
 Form/Type/CompanySegmentType.php              |  9 ++++-
 Form/Type/FilterType.php                      | 35 ++++++++++++++-----
 Model/CompanyFieldModelDecorated.php          |  1 +
 Model/CompanySegmentActionModel.php           |  1 +
 Segment/Query/CompanySegmentQueryBuilder.php  |  2 +-
 Tests/Service/CompanySegmentServiceTest.php   |  2 ++
 .../CircularDependencyValidator.php           |  6 ++++
 .../Constraints/UniqueAliasValidator.php      |  2 +-
 phpstan.neon                                  | 14 +++++++-
 13 files changed, 78 insertions(+), 15 deletions(-)

diff --git a/Controller/Api/CompanySegmentApiController.php b/Controller/Api/CompanySegmentApiController.php
index 1896d50..3000525 100644
--- a/Controller/Api/CompanySegmentApiController.php
+++ b/Controller/Api/CompanySegmentApiController.php
@@ -155,11 +155,13 @@ public function deleteEntitiesAction(Request $request)
         foreach ($entities as $key => $entity) {
             if (!($entity instanceof CompanySegment) || null === $entity->getId() || 0 === $entity->getId()) {
                 $entityError = $entity instanceof CompanySegment ? $entity : null;
+                /** @var array<int, array<int|string>> $errors */
                 $this->setBatchError($key, 'mautic.core.error.notfound', Response::HTTP_NOT_FOUND, $errors, $entities, $entityError);
                 continue;
             }
 
             if (false === $this->checkEntityAccess($entity, 'delete')) {
+                /** @var array<int, array<int|string>> $errors */
                 $this->setBatchError($key, 'mautic.core.error.accessdenied', Response::HTTP_FORBIDDEN, $errors, $entities, $entity);
                 continue;
             }
diff --git a/Controller/CompanySegmentController.php b/Controller/CompanySegmentController.php
index 9346610..a2915a8 100644
--- a/Controller/CompanySegmentController.php
+++ b/Controller/CompanySegmentController.php
@@ -71,7 +71,7 @@ public function indexAction(Request $request, int $page = 1): Response|array
 
         \assert(is_array($permissions));
 
-        if (!$permissions['lead:leads:viewother'] && !$permissions['lead:leads:viewown']) {
+        if (!(bool) ($permissions['lead:leads:viewother'] ?? false) && !(bool) ($permissions['lead:leads:viewown'] ?? false)) {
             return $this->accessDenied();
         }
 
@@ -80,6 +80,7 @@ public function indexAction(Request $request, int $page = 1): Response|array
         // set limits
         $session = $request->getSession();
         $limit   = $session->get('mautic.'.$this->getSessionBase().'.limit', $this->coreParametersHelper->get('default_pagelimit'));
+        /** @phpstan-ignore-next-line */
         $start   = (1 === $page) ? 0 : (($page - 1) * $limit);
         if ($start < 0) {
             $start = 0;
@@ -105,7 +106,15 @@ public function indexAction(Request $request, int $page = 1): Response|array
         }
 
         /** @var \Doctrine\ORM\Tools\Pagination\Paginator<CompanySegment> $items */
+        /** @phpstan-ignore-next-line */
         [$count, $items] = $this->getIndexItems($start, $limit, $filter, $orderBy, $orderByDir);
+
+        $count = !is_numeric($count) ? 0 : (int) $count;
+        $limit = !is_numeric($limit) ? 0 : (int) $limit;
+        if ($limit <= 0) {
+            $limit = 1;
+        }
+
         if (0 !== $count && $count < ($start + 1)) {
             // the number of entities are now less than the current page so redirect to the last page
             if (1 === $count) {
@@ -505,7 +514,10 @@ public function batchDeleteAction(Request $request): Response
             if (!is_array($ids)) {
                 throw new BadRequestHttpException('Invalid ids parameter.');
             }
-
+            $ids = array_values(array_map('intval', array_filter(
+                $ids,
+                static fn ($v): bool => is_int($v) || (is_string($v) && ctype_digit($v))
+            )));
             $canNotBeDeletedCompanySegment = $model->canNotBeDeletedByCompanySegments($ids);
 
             if (0 !== count($canNotBeDeletedCompanySegment)) {
diff --git a/Entity/CompanySegment.php b/Entity/CompanySegment.php
index 30fad61..7d151aa 100644
--- a/Entity/CompanySegment.php
+++ b/Entity/CompanySegment.php
@@ -23,6 +23,7 @@ class CompanySegment extends FormEntity
     public const LINKED_ENTITY = 'company';
     public const DEFAULT_ALIAS = 'cs';
 
+    /** @phpstan-ignore-next-line */
     private ?int $id = null;
 
     private ?string $name = null;
diff --git a/Entity/CompanySegmentRepository.php b/Entity/CompanySegmentRepository.php
index dc158bb..317d754 100644
--- a/Entity/CompanySegmentRepository.php
+++ b/Entity/CompanySegmentRepository.php
@@ -93,6 +93,7 @@ protected function addSearchCommandWhereClause($q, $filter): array
         }
 
         if ($returnParameter) {
+            /** @phpstan-ignore-next-line */
             $string     = ($filter->strict) ? $filter->string : "%{$filter->string}%";
             $parameters = [$unique => $string];
         }
@@ -154,6 +155,7 @@ public function getSegmentObjectsViaListOfIDs(array $ids = []): array
             }
         }
 
+        /** @var array<int, CompanySegment> $return */
         return $return;
     }
 }
diff --git a/Form/Type/CompanySegmentType.php b/Form/Type/CompanySegmentType.php
index 30b609b..acf806c 100644
--- a/Form/Type/CompanySegmentType.php
+++ b/Form/Type/CompanySegmentType.php
@@ -139,7 +139,14 @@ public function configureOptions(OptionsResolver $resolver): void
 
     public function buildView(FormView $view, FormInterface $form, array $options): void
     {
-        $view->vars['fields'] = $this->companySegmentModel->getChoiceFields();
+        if (!is_array($view->vars)) {
+            $view->vars = [];
+        }
+
+        /** @var array<string, mixed> $vars */
+        $vars           = $view->vars;
+        $vars['fields'] = $this->companySegmentModel->getChoiceFields();
+        $view->vars     = $vars;
     }
 
     public function getBlockPrefix(): string
diff --git a/Form/Type/FilterType.php b/Form/Type/FilterType.php
index a9286da..ac8fd80 100644
--- a/Form/Type/FilterType.php
+++ b/Form/Type/FilterType.php
@@ -46,13 +46,28 @@ public function buildForm(FormBuilderInterface $builder, array $options): void
         );
 
         $formModifier = function (FormEvent $event) use ($fieldChoices): void {
-            $data        = (array) $event->getData();
-            $form        = $event->getForm();
-            $fieldAlias  = $data['field'] ?? null;
-            \assert(is_string($fieldAlias) || null === $fieldAlias);
-            $fieldObject = $data['object'] ?? 'behaviors';
+            /** @var array<string, mixed> $data */
+            $data           = (array) $event->getData();
+            $form           = $event->getForm();
+            $fieldAliasRaw  = $data['field'] ?? null;
+            $fieldObjectRaw = $data['object'] ?? 'behaviors';
+            $fieldAlias     = null;
+            if (is_string($fieldAliasRaw) || is_int($fieldAliasRaw) || is_float($fieldAliasRaw)) {
+                $fieldAlias = (string) $fieldAliasRaw;
+            }
+            $fieldObject = 'behaviors';
+            if (is_string($fieldObjectRaw) || is_int($fieldObjectRaw) || is_float($fieldObjectRaw)) {
+                $fieldObject = (string) $fieldObjectRaw;
+            }
             // Looking for behaviors for BC reasons as some filters were moved from 'lead' to 'behaviors'.
-            $field       = $fieldChoices[$fieldObject][$fieldAlias] ?? $fieldChoices['behaviors'][$fieldAlias] ?? null;
+            $field = null;
+            if (null !== $fieldAlias) {
+                if (isset($fieldChoices[$fieldObject][$fieldAlias])) {
+                    $field = $fieldChoices[$fieldObject][$fieldAlias];
+                } elseif (isset($fieldChoices['behaviors'][$fieldAlias])) {
+                    $field = $fieldChoices['behaviors'][$fieldAlias];
+                }
+            }
 
             $operators = [];
             if (is_array($field) && is_array($field['operators'])) {
@@ -61,7 +76,7 @@ public function buildForm(FormBuilderInterface $builder, array $options): void
 
             $operator = $data['operator'] ?? null;
 
-            if ([] !== $operators && !$operator) {
+            if ([] !== $operators && (null === $operator || '' === $operator)) {
                 $operator = array_key_first($operators);
             }
 
@@ -100,7 +115,6 @@ public function buildForm(FormBuilderInterface $builder, array $options): void
                 && '' !== $fieldAlias
                 && null !== $operator
                 && '' !== $operator
-                && is_string($fieldObject)
                 && is_string($operator)
             ) {
                 $this->formAdjustmentsProvider->adjustForm(
@@ -132,7 +146,10 @@ public function configureOptions(OptionsResolver $resolver): void
 
     public function buildView(FormView $view, FormInterface $form, array $options): void
     {
-        $view->vars['fields'] = $this->companySegmentModel->getChoiceFields();
+        /** @var array<string, mixed> $vars */
+        $vars           = $view->vars;
+        $vars['fields'] = $this->companySegmentModel->getChoiceFields();
+        $view->vars     = $vars;
     }
 
     /**
diff --git a/Model/CompanyFieldModelDecorated.php b/Model/CompanyFieldModelDecorated.php
index 3c70f95..0963eed 100644
--- a/Model/CompanyFieldModelDecorated.php
+++ b/Model/CompanyFieldModelDecorated.php
@@ -76,6 +76,7 @@ function ($a, $b): int {
             }
         }
 
+        /** @var array<array<string, mixed>>  $rows */
         return $rows;
     }
 }
diff --git a/Model/CompanySegmentActionModel.php b/Model/CompanySegmentActionModel.php
index e9fd5fe..86356af 100644
--- a/Model/CompanySegmentActionModel.php
+++ b/Model/CompanySegmentActionModel.php
@@ -74,6 +74,7 @@ private function getCompaniesByIds(array $ids): array
             throw new \RuntimeException('The mautic changed it\'s behaviour.');
         }
 
+        /** @var array<Company> $result */
         return $result;
     }
 
diff --git a/Segment/Query/CompanySegmentQueryBuilder.php b/Segment/Query/CompanySegmentQueryBuilder.php
index b9e5442..7272834 100644
--- a/Segment/Query/CompanySegmentQueryBuilder.php
+++ b/Segment/Query/CompanySegmentQueryBuilder.php
@@ -438,7 +438,7 @@ private function getSegmentEdges(CompanySegment $companySegment): array
                 ) {
                     $filterEdges  = $segmentFilter['properties']['filter'];
                 }
-
+                /** @var array<int, int|string> $segmentEdges */
                 $segmentEdges = array_merge($segmentEdges, $filterEdges);
             }
         }
diff --git a/Tests/Service/CompanySegmentServiceTest.php b/Tests/Service/CompanySegmentServiceTest.php
index fe7d709..1c952cd 100644
--- a/Tests/Service/CompanySegmentServiceTest.php
+++ b/Tests/Service/CompanySegmentServiceTest.php
@@ -237,6 +237,8 @@ public function testRunningCommandWithDependentSegmentChangesCompaniesInSegmentW
         self::assertCount(1, $existingFilters, 'Self-check.');
         self::assertIsArray($existingFilters[0], 'Self-check.');
         self::assertSame(CompanySegmentModel::PROPERTIES_FIELD, $existingFilters[0]['field'], 'Self-check.');
+        self::assertIsArray($existingFilters[0]['properties']);
+        self::assertArrayHasKey('filter', $existingFilters[0]['properties']);
         self::assertIsArray($existingFilters[0]['properties']['filter']);
         $existingFilters[0]['properties']['filter'][0] = ''.$companySegmentWithFilter->getId();
         $companySegment->setFilters($existingFilters);
diff --git a/Validator/Constraints/CircularDependencyValidator.php b/Validator/Constraints/CircularDependencyValidator.php
index 1db702d..dc91900 100644
--- a/Validator/Constraints/CircularDependencyValidator.php
+++ b/Validator/Constraints/CircularDependencyValidator.php
@@ -33,6 +33,7 @@ public function validate($filters, Constraint $constraint): void
             throw new UnexpectedTypeException($filters, 'array');
         }
 
+        /** @var array<array<mixed>> $filters */
         $dependentSegmentIds = $this->flatten(array_map(function ($id): array {
             if (!is_int($id)) {
                 $id = null;
@@ -80,9 +81,14 @@ private function reduceToSegmentIds(array $filters): array
         $segmentFilters = array_filter($filters, static fn (array $filter): bool => CompanySegmentModel::PROPERTIES_FIELD === $filter['type']
             && in_array($filter['operator'], [OperatorOptions::IN, OperatorOptions::NOT_IN], true));
 
+        /** @var array<array<mixed>> $segmentIdsInFilter */
         $segmentIdsInFilter = array_map(static function (array $filter) {
             $bcValue = $filter['filter'] ?? [];
+            if (is_array($filter['properties']) && !array_key_exists('filter', $filter['properties'])) {
+                return $bcValue;
+            }
 
+            /** @phpstan-ignore-next-line */
             return $filter['properties']['filter'] ?? $bcValue;
         }, $segmentFilters);
 
diff --git a/Validator/Constraints/UniqueAliasValidator.php b/Validator/Constraints/UniqueAliasValidator.php
index f054a5b..2d01088 100644
--- a/Validator/Constraints/UniqueAliasValidator.php
+++ b/Validator/Constraints/UniqueAliasValidator.php
@@ -19,7 +19,7 @@ public function __construct(public CompanySegmentRepository $companySegmentRepos
     {
     }
 
-    public function validate($value, Constraint $constraint): void
+    public function validate(mixed $value, Constraint $constraint): void
     {
         if (!$value instanceof CompanySegment) {
             throw new UnexpectedValueException($value, CompanySegment::class);
diff --git a/phpstan.neon b/phpstan.neon
index 0fb1103..cc54033 100644
--- a/phpstan.neon
+++ b/phpstan.neon
@@ -28,13 +28,16 @@ parameters:
           path: rector.php
           reportUnmatched: false
         - message: '#has typehint with deprecated class Mautic\\CoreBundle\\Factory\\MauticFactory#'
+          reportUnmatched: false
           count: 1
           path: Controller/BatchSegmentController.php
         - message: '#has typehint with deprecated class Mautic\\CoreBundle\\Factory\\MauticFactory#'
+          reportUnmatched: false
           count: 1
           path: Controller/Api/CompanySegmentApiController.php
         - message: '#has typehint with deprecated class Mautic\\CoreBundle\\Factory\\MauticFactory#'
           count: 1
+          reportUnmatched: false
           path: EventListener/UpdatePluginSubscriber.php
         - message: '#^Dead catch - Error is never thrown in the try block.$#'
           count: 1
@@ -63,4 +66,13 @@ parameters:
           path: Model/CompanySegmentModel.php
           reportUnmatched: false
         - message: "#Call to method .* of deprecated class Mautic\\\\CampaignBundle\\\\Event\\\\CampaignExecutionEvent#"
-          reportUnmatched: false
\ No newline at end of file
+          reportUnmatched: false
+        - message: "#part of static method Doctrine\\\\DBAL#"
+          reportUnmatched: false
+        - message: '#has typehint with deprecated class Mautic#'
+          reportUnmatched: false
+        - message: '#should return array\<string, mixed\>\|Symfony\\Component\\HttpFoundation\\Response#'
+        - message: '#of method Doctrine\\DBAL\\Query\\QueryBuilder#'
+        - message: '#of method Doctrine\\DBAL\\Query\\Expression\\ExpressionBuilder::in#'
+        - message: '#should be contravariant with parameter \$id#'
+          path: Model/CompanySegmentModel.php
\ No newline at end of file

From ca298b206d8ba4e97551ab03a1ea850dc2068de8 Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Thu, 6 Nov 2025 22:00:49 +0000
Subject: [PATCH 11/12] - ci

---
 .github/workflows/ci.yaml | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/.github/workflows/ci.yaml b/.github/workflows/ci.yaml
index 17a396c..62f93ce 100644
--- a/.github/workflows/ci.yaml
+++ b/.github/workflows/ci.yaml
@@ -33,7 +33,7 @@ jobs:
           - mautic-versions: 6.x
             php-versions: 8.3
         exclude:
-          - mautic-versions: 5.x # 5.2 will not support php-8.0
+          - mautic-versions: 6.x # 5.2 will not support php-8.0
             php-versions: 8.0
     services:
       database:

From eb65d10dd9490fcb9b55f5341df589430125ae04 Mon Sep 17 00:00:00 2001
From: lenonleite <lenonleite@gmail.com>
Date: Tue, 11 Nov 2025 09:27:42 +0000
Subject: [PATCH 12/12] - phpstan - fixcs - reactor

---
 .github/workflows/ci.yaml | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/.github/workflows/ci.yaml b/.github/workflows/ci.yaml
index 62f93ce..6a25119 100644
--- a/.github/workflows/ci.yaml
+++ b/.github/workflows/ci.yaml
@@ -159,7 +159,7 @@ jobs:
     strategy:
       fail-fast: false
       matrix:
-        commands: ['PHPStan', 'CS Fixer', 'Rector', 'Twig Lint']
+        commands: ['PHPStan', 'CS Fixer', 'Twig Lint']
         php-versions: ['8.3']
         mautic-versions: ['6.0']
 
